Focus: Audio-visual pairing | Found: 3 items (🔴1 🟡1 🟢1 ⚪0) | Proposals: 3 | Applied: 0

### [🔴] Card hover lacks audio anchor
- **What happens now**: Hovering over a card visually scales it up, but there is no audio feedback to anchor the interaction.
- **Why it feels off**: The visual state change (hover) is completely silent, breaking the physical illusion of touching a physical card. Missing audio anchor.
- **Location**: `src/app/components/ui/card/Card.tsx:403` (inside `onHoverStart`)
- **Proposed change**: Add `playSFX('hover_tick')` inside `onHoverStart`.
- **Why this works**: A subtle, quiet tick sound exactly as the card starts scaling confirms the pointer's hit target, adding a snappy, tactile feel.
- **Confidence**: High

### [🟡] Card expand/zoom lacks audio anchor
- **What happens now**: Clicking the card expands it to show the full definition. It reads the title via TTS (`tts.speak`), but there is no sound effect tied to the actual expanding animation.
- **Why it feels off**: The visual transition is large and important (zoom), but it sounds weightless. The TTS speech overlaps with silence, missing a satisfying physical snap. Missing audio anchor.
- **Location**: `src/app/components/ui/card/Card.tsx:382` (inside `onClick`)
- **Proposed change**: Add `playSFX('card_flip_heavy')` or `playSFX('swoosh')` exactly when `startAnimation()` is called to trigger the zoom.
- **Why this works**: Grounding the large visual transition with a 'swoosh' or 'heavy flip' sound makes the card feel heavier and the action more deliberate.
- **Confidence**: High

### [🟢] Card drop target lacks drop feedback
- **What happens now**: Dropping another item onto the card (via `useDrop`) visually accepts it, but there is no auditory confirmation.
- **Why it feels off**: Successful interactions without sound feel broken or unrewarding.
- **Location**: `src/app/components/ui/card/Card.tsx:335` (`useDrop` implementation)
- **Proposed change**: Add `playSFX('thud')` or `playSFX('drop_success')` inside the `drop` callback when an item successfully lands on the card.
- **Why this works**: A satisfying 'thud' signals to the user that the drag-and-drop action has resolved successfully, providing closure to the physical interaction.
- **Confidence**: High

Tomorrow's focus: Canvas camera feel
