Focus: Ambient/idle motion | Found: 4 items (🔴0 🟡3 🟢1 ⚪0) | Proposals: 4 | Applied: 0

### [🟡] LevelUpOverlay radial background pulse feels default
- **What happens now**: The background has a fast, uniform, hard-coded CSS `animate-pulse` that feels cheap.
- **Why it feels off**: Easing is default (linear/ease-in-out), lacking the organic "breathing" rhythm (anticipation and slow fade).
- **Location**: `src/app/components/ui/system/LevelUpOverlay.tsx:37`
- **Proposed change**:
  - Current: `className="... animate-pulse"`
  - Proposed: Use framer-motion instead. Remove `animate-pulse` and wrap the div in `<motion.div animate={{ opacity: [0.3, 0.7, 0.3], scale: [0.95, 1.05, 0.95] }} transition={{ duration: 4, repeat: Infinity, ease: "easeInOut" }} />`
- **Why this works**: Slow, eased breathing feels magical and deliberate, unlike standard fast CSS pulsing.
- **Confidence**: High

### [🟢] RewardCinematicOverlay Sparkles icon uses default pulse
- **What happens now**: The Sparkles icon flashes rigidly using Tailwind's default `animate-pulse`.
- **Why it feels off**: Mechanical easing makes it look like a loading state rather than a magical reward intro.
- **Location**: `src/app/components/ui/visual/RewardCinematicOverlay.tsx:57`
- **Proposed change**:
  - Current: `<Sparkles className="... animate-pulse" />`
  - Proposed: Remove `animate-pulse`, use `motion.div` wrapper: `<motion.div animate={{ scale: [1, 1.2, 1], opacity: [0.5, 1, 0.5] }} transition={{ duration: 2, repeat: Infinity, ease: "easeInOut" }}> <Sparkles ... /> </motion.div>`
- **Why this works**: Slower, scaled pulsing feels like glowing energy rather than a blinking alert.
- **Confidence**: High

### [🟡] RewardCinematicOverlay Echo Reveal background uses default pulse
- **What happens now**: The glowing blur effect behind the Echo Reveal flashes abruptly.
- **Why it feels off**: The `animate-pulse` is too fast for a majestic "manifestation" moment.
- **Location**: `src/app/components/ui/visual/RewardCinematicOverlay.tsx:111`
- **Proposed change**:
  - Current: `<div className="... animate-pulse" />`
  - Proposed: Replace with `<motion.div className="..." animate={{ opacity: [0.4, 0.8, 0.4], scale: [1.4, 1.6, 1.4] }} transition={{ duration: 3, repeat: Infinity, ease: "easeInOut" }} />` and remove `animate-pulse`.
- **Why this works**: A slow, expanding glow creates a feeling of power and mystery.
- **Confidence**: High

### [🟢] SynthesisCircle decorative dashed ring is completely linear
- **What happens now**: The dashed ring spins uniformly at a constant speed forever.
- **Why it feels off**: Linear easing feels robotic; it lacks the arcane, slightly organic flow of magical machinery.
- **Location**: `src/app/components/ui/visual/SynthesisCircle.tsx:142`
- **Proposed change**:
  - Current: `className="... animate-[spin_60s_linear_infinite]"`
  - Proposed: Change to a custom animation or motion wrapper with staggered rotation: `<motion.div animate={{ rotate: [0, 180, 360] }} transition={{ duration: 60, repeat: Infinity, ease: "linear" }} />` (Actually, if it's meant to be arcane, perhaps a slight ease or varied speed, but let's stick to CSS but add pulsing opacity for life): `<motion.div animate={{ opacity: [0.3, 0.6, 0.3], rotate: 360 }} transition={{ opacity: { duration: 4, repeat: Infinity, ease: "easeInOut" }, rotate: { duration: 60, repeat: Infinity, ease: "linear" } }} className="... border-dashed border-white/5" />` (and remove tailwind animate-[spin...]).
- **Why this works**: Adding a breathing opacity layer to the rotation makes it feel alive, not just a loading spinner.
- **Confidence**: Medium

Tomorrow's focus: Audio-visual pairing
