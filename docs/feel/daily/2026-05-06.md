Focus: Success/failure feedback | Found: 4 items (🔴0 🟡3 🟢1 ⚪0) | Proposals: 4 | Applied: 0

### [🟡] SynthesisCircle Error Shake missing
- **What happens now**: When synthesis fails (`synthState === 'error'`), the circle just displays red text at the bottom. The failure feels silent and easily missed.
- **Why it feels off**: Missing Responsiveness / Secondary motion. A magical failure should feel volatile.
- **Location**: `src/app/components/ui/visual/SynthesisCircle.tsx` (around line 115)
- **Proposed change**: Add a shake animation to the main container when `synthState === 'error'`: `animate={{ x: [0, -10, 10, -10, 10, 0] }}` with `transition={{ duration: 0.4 }}`. Also, pair with an audio cue: `playSFX('error_buzz')`.
- **Why this works**: A rapid lateral shake (error shake) physically communicates rejection and draws the user's eye to the failure state.
- **Confidence**: High

### [🟡] SynthesisCircle Success Burst lacks climax
- **What happens now**: When synthesis succeeds (`synthState === 'success'`), the ingredient cards just pop out of existence (ejected) without any fanfare, and the new card appears.
- **Why it feels off**: Missing Anticipation and Follow-through. A successful alchemy combining two cards should have a visual climax.
- **Location**: `src/app/components/ui/visual/SynthesisCircle.tsx` (around line 105)
- **Proposed change**: Before ejecting the cards, flash the central container: `animate={{ scale: [1, 1.2, 1], filter: ['brightness(1)', 'brightness(2)', 'brightness(1)'] }}` with `transition={{ duration: 0.5, ease: 'easeOut' }}`. Pair with `playSFX('success_chime')`.
- **Why this works**: A brief expansion (squash & stretch) and brightness flash creates a visual "pop" that celebrates the success and justifies the transformation of the cards.
- **Confidence**: High

### [🟡] RewardCinematicOverlay Tally Card Entry is sluggish
- **What happens now**: The XP and Resonance tally cards float in linearly from the sides (`x: -50` to `x: 0`) with default motion settings, feeling like a basic PowerPoint slide.
- **Why it feels off**: Linear easing. Rewards should feel exciting and energetic.
- **Location**: `src/app/components/ui/visual/RewardCinematicOverlay.tsx` (tally section, lines 72, 83)
- **Proposed change**: Change the transition to use spring physics: `transition={{ type: 'spring', damping: 12, stiffness: 200 }}`.
- **Why this works**: Spring physics adds Overshoot, making the cards feel like they are snapping into place with weight and momentum, increasing the perceived value of the reward.
- **Confidence**: High

### [🟢] LevelUpOverlay Transition Arrow is static
- **What happens now**: In the level up overlay, the "previous level → new level" text just fades in statically together.
- **Why it feels off**: Missing Staging. The transition from old to new is the core story of leveling up, but it's presented as a flat fact.
- **Location**: `src/app/components/ui/system/LevelUpOverlay.tsx` (around line 80)
- **Proposed change**: Wrap the elements in a staggered motion setup. Make the old level slide away slightly left, the arrow fade in, and the new level scale up from 0.8 with a slight overshoot: `animate={{ scale: [0.8, 1.2, 1] }}`.
- **Why this works**: Animating the sequence reinforces the narrative of growth—leaving the old behind and powerfully stepping into the new.
- **Confidence**: Medium

Tomorrow's focus: Ambient/idle motion
