Focus: Card interactions | Found: 3 items (🔴0 🟡2 🟢1 ⚪0) | Proposals: 3 | Applied: 0

### [🟡] Card Flip Spring is too loose
- **What happens now**: The card flip feels slightly sluggish and floaty, rotating slower than the scale animation resolves.
- **Why it feels off**: Easing and Responsiveness violation. The damping is too low and stiffness is not quite high enough for a snappy mechanical flip.
- **Location**: `src/app/components/persona/default/Card.persona.default.tsx:287`
- **Proposed change**: `flip: { stiffness: 220, damping: 24 }` → `flip: { stiffness: 280, damping: 28 }`
- **Why this works**: Increasing stiffness makes the turn faster while raising damping prevents prolonged wobbling, anchoring the flip visually.
- **Confidence**: High

### [🟡] Drag Hover Lift missing anticipation
- **What happens now**: Hovering over a card instantly snaps it to `scaleSpring.set(1.08)` without any lead-in, and dragging forces `1.15`.
- **Why it feels off**: Anticipation violation. The lift feels abrupt because the `1.08` scale engages simultaneously with the drag cursor switch.
- **Location**: `src/app/components/ui/card/Card.tsx:410` and `src/app/hooks/useCardDrag.ts:75`
- **Proposed change**: Consider adding a very brief delay or a tighter curve (e.g. `scale: { stiffness: 400, damping: 30 }` just for hover state) to give the lift an explosive start.
- **Why this works**: A fast lift mimics physical suction or picking up a heavy object, adding weight.
- **Confidence**: Medium

### [🟢] Drag Drop lacks Follow-through audio
- **What happens now**: When a drag ends and the card scales back down from `1.15` to `1.0`, there is a visual settle but no audio cue marking the release.
- **Why it feels off**: Audio anchor violation. Dropping an item has a visual "thud" without the accompanying sound.
- **Location**: `src/app/hooks/useCardDrag.ts:130`
- **Proposed change**: Call `tts.speak('thud')` or a dedicated `playSFX('cardDrop')` in the `if (last)` block of `useDrag`.
- **Why this works**: An audio anchor on release confirms the interaction is over and pairs with the visual settle.
- **Confidence**: High

Tomorrow's focus: Screen/state transitions
