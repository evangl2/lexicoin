Focus: Screen/state transitions | Found: 4 items (🔴0 🟡3 🟢1 ⚪0) | Proposals: 4 | Applied: 0

### [🟡] ConfigMenu slide-up is harsh and abrupt
- **What happens now**: The configuration menu slides up from the bottom but stops dead upon arrival, feeling heavy and robotic.
- **Why it feels off**: High damping eliminates any follow-through or overshoot, violating organic motion easing.
- **Location**: `src/app/components/ui/shell/ConfigMenu.tsx:340` (`transition={{ type: "spring", stiffness: 300, damping: 30 }}`)
- **Proposed change**: `transition={{ type: "spring", stiffness: 300, damping: 30 }}` → `transition={{ type: "spring", stiffness: 300, damping: 22 }}`
- **Why this works**: Reducing damping allows the menu to slightly overshoot its target before settling, giving it a snappy, physical presence as if stopping against a slight bouncy resistance.
- **Confidence**: High

### [🟡] LevelUpOverlay backdrop appears instantly
- **What happens now**: When the level up overlay appears, the dark semi-transparent background flashes instantly into existence while the content animates.
- **Why it feels off**: The background opacity lacks an explicit duration/easing, causing it to default to a 0ms transition which breaks the staging and immersion of the event.
- **Location**: `src/app/components/ui/system/LevelUpOverlay.tsx:40` (`className="fixed inset-0... backdrop-blur-md"`)
- **Proposed change**: Add `transition={{ duration: 0.4, ease: "easeOut" }}` to the background `motion.div`.
- **Why this works**: A gentle fade-in for the backdrop smooths the scene change, focusing attention gradually rather than jarring the eyes.
- **Confidence**: High

### [🟡] GrimoireOverlay enter animation feels rigid
- **What happens now**: The grimoire book appears by scaling up and rotating slightly, but it lacks a defined spring transition, making it feel somewhat linear and generic.
- **Why it feels off**: Missing a defined easing/spring curve means it relies on default Framer Motion transitions which don't quite sell the weight of a heavy book.
- **Location**: `src/app/components/ui/visual/GrimoireOverlay.tsx:42` (`<motion.div initial={{ scale: 0.9, y: 20, rotateX: 10 }}`)
- **Proposed change**: Add `transition={{ type: 'spring', damping: 20, stiffness: 150 }}` to the book container.
- **Why this works**: Using a spring transition adds physical weight, making the heavy manuscript feel like it's settling solidly onto the screen.
- **Confidence**: Medium

### [🟢] LibraryInterface transition is too linear
- **What happens now**: Entering the library cross-fades and slightly scales the view. It's functional but lacks a sense of depth or grandness.
- **Why it feels off**: The `scale` values (`1.1` to `1` to `0.95`) combined with default easing make it feel like a simple zoom rather than traversing into a vast repository.
- **Location**: `src/app/components/ui/visual/LibraryInterface.tsx:150` (`initial={{ opacity: 0, scale: 1.1 }} animate={{ opacity: 1, scale: 1 }} exit={{ opacity: 0, scale: 0.95 }}`)
- **Proposed change**: Add `transition={{ type: "spring", stiffness: 100, damping: 25, mass: 1.2 }}` and change scale to `initial={{ opacity: 0, scale: 1.05 }}`.
- **Why this works**: A slower, heavier spring gives a "cinematic" entrance feel, suggesting stepping into a large, quiet space.
- **Confidence**: Medium

Tomorrow's focus: Success/failure feedback
