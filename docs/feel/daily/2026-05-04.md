Focus: Card interactions | Found: 3 items (🔴0 🟡2 🟢1 ⚪0) | Proposals: 2 | Applied: 1

### [🟢] DragLayer performance optimization (hardcoded will-change)
- **What happens now**: The drag preview element always has `will-change-transform` applied.
- **Why it feels off**: While not a visible feel issue, hardcoding `will-change: transform` on frequently spawned or highly dynamic elements like drag previews causes excessive GPU memory allocation and can lead to compositing stutter on some devices (Responsiveness / Staging).
- **Location**: `src/app/components/ui/canvas/DragLayer.tsx:73`
- **Proposed change**: Remove `className="absolute top-0 left-0 will-change-transform"` → `className="absolute top-0 left-0"`.
- **Why this works**: Modern browsers automatically promote actively animated elements to GPU layers when dragging; relying on browser optimizations prevents VRAM bloat and keeps the drag feel smooth.
- **Confidence**: High
- **Note**: Auto-applied as it is a pure additive perf fix hook per instructions.

### [🟡] DragPreviewCard appears instantly with no anticipation or scale transition when dragging starts
- **What happens now**: When dragging begins, the drag preview card simply flashes into existence at its full scale/opacity without any transition.
- **Why it feels off**: Lacks anticipation. Spawning items from thin air without a slight scale-up makes the action feel abrupt and disconnected from the user's input.
- **Location**: `src/app/components/ui/card/DragPreviewCard.tsx:103` (Container `div`)
- **Proposed change**: Convert the container to `motion.div` and add `initial={{ scale: 0.8, opacity: 0 }}` and `animate={{ scale: 1, opacity: 1 }}` along with a snappy spring `transition={{ type: "spring", stiffness: 350, damping: 25 }}`.
- **Why this works**: Adding a small, fast scale up provides anticipation, giving the card physical substance as it pops off the table and into the user's hand.
- **Confidence**: High

### [🟡] Card mouseTilt is overdamped and feels too rigid when hovering
- **What happens now**: Moving the mouse over the card tilts it, but it resists motion heavily and feels almost robotic when returning to neutral.
- **Why it feels off**: The high damping value limits organic flow and reduces the feeling of a lightweight paper card pivoting on an axis (Easing / Responsiveness).
- **Location**: `src/app/components/persona/default/Card.persona.default.tsx:301` (`springs.mouseTilt`)
- **Proposed change**: `{ damping: 50, stiffness: 120, mass: 1 }` → `{ damping: 40, stiffness: 150, mass: 1 }`
- **Why this works**: Slightly increasing the stiffness while lowering the damping makes the tilt faster to respond to the mouse but lets it settle with a subtle organic looseness, enhancing the 3D depth illusion.
- **Confidence**: Medium

Tomorrow's focus: Screen/state transitions
