Focus: Audio-visual pairing | Found: 4 items (🔴1 🟡2 🟢1 ⚪0) | Proposals: 4 | Applied: 0

### [🔴] LevelUpOverlay missing audio anchor
- **What happens now**: The level up overlay pops in completely silently despite large visual impact (spring scale from 0.5 to 1, full screen takeover).
- **Why it feels off**: Audio anchor. A sudden full-screen visual interruption without a corresponding audio cue feels ungrounded and abrupt rather than celebratory.
- **Location**: `src/app/components/ui/system/LevelUpOverlay.tsx:28`
- **Proposed change**: Add `const { playSFX } = useAudio();` hook. In the messageBus subscriber callback for `LEVEL_UP`, call `playSFX('levelUp')` immediately before setting the local state.
- **Why this works**: Connecting the visual impact (spring pop) to a sound makes the reward feel tangible and solid.
- **Confidence**: High

### [🟡] RewardCinematicOverlay sequential steps lack auditory staging
- **What happens now**: The cinematic overlay proceeds through 'intro' -> 'tally' -> 'echo' steps purely on timeouts (1000ms, 3000ms, 5500ms) with no sound cues.
- **Why it feels off**: Staging. Complex sequences of visual reveals need an auditory track to guide the user's attention and signify progression through the 'ritual'.
- **Location**: `src/app/components/ui/visual/RewardCinematicOverlay.tsx:16`
- **Proposed change**: Introduce `const { playSFX } = useAudio();` and add sound cues to the timeouts. Play a 'ritualStart' at mount, 'tallyPop' at the 1000ms step, and 'echoReveal' at the 3000ms step. Also play 'ritualComplete' at the 5500ms mark.
- **Why this works**: Providing audio cues synchronously with state changes helps stage the user's attention and reinforces the pacing.
- **Confidence**: High

### [🟡] SynthesisCircle 'Synthesize' button click lacks responsiveness
- **What happens now**: When the synthesize button is clicked, it visually transitions to a spinning loader and dispatches the synthesis event, but provides no immediate audio feedback.
- **Why it feels off**: Responsiveness. A heavy action (triggering an AI/backend call) should acknowledge the user's input instantly, especially when visual feedback may take a frame or two to update.
- **Location**: `src/app/components/ui/visual/SynthesisCircle.tsx:130`
- **Proposed change**: Add `const { playSFX } = useAudio();` to the component. Inside the `handleSynthesize` function, before updating state, call `playSFX('synthesizeClick')`.
- **Why this works**: An immediate auditory confirmation reassures the user that the system received their command.
- **Confidence**: High

### [🟢] Card hover and context menu lack audio polish
- **What happens now**: Dragging and TTS on definition click work, but interacting with the card (hovering, toggling the context menu/flip) happens silently.
- **Why it feels off**: Secondary motion/feedback. In a tactile card game interface, hovering over cards or flipping them should feel like handling physical objects.
- **Location**: `src/app/components/ui/card/Card.tsx:185`
- **Proposed change**: Call `playSFX('cardHover')` on `onPointerEnter`, and `playSFX('cardFlip')` inside `onContextMenu` when `isFlipped` state toggles.
- **Why this works**: Adding subtle physical sounds to card interactions enhances the immersion and perceived weight of the interface.
- **Confidence**: Medium

Tomorrow's focus: Canvas camera feel
