Focus: Text/number animation | Found: 6 items (🔴1 🟡4 🟢1 ⚪0) | Proposals: 6 | Applied: 0

### [🟡] ProgressionHUD Experience Bar Lacks Impact
- **What happens now**: The experience bar fills up smoothly when XP is gained, but there is no visual flare or celebration on the bar itself, making progress feel a bit dry.
- **Why it feels off**: Missing anticipation and follow-through. It uses a standard spring, but there's no secondary motion or glow when a chunk of XP is added.
- **Location**: `src/app/components/ui/shell/ProgressionHUD.tsx:75`
- **Proposed change**: Add a momentary bright flash to the bar's `boxShadow` or a slight scale bump when `xp` increases significantly. `scale: 1 → 1.05 → 1` on XP gain.
- **Why this works**: A brief expansion (squash & stretch) makes the bar feel like it actually "swallowed" the new experience points.
- **Confidence**: High

### [🔴] ProgressionHUD Level Increment Lacks Drama
- **What happens now**: When the level number increases in the top-left HUD, the number just changes instantly without any transition.
- **Why it feels off**: Missing anticipation and staging. Leveling up is a key moment, but the HUD number swap is completely static.
- **Location**: `src/app/components/ui/shell/ProgressionHUD.tsx:55`
- **Proposed change**: Wrap the level number in an `<AnimatePresence>` or use a motion component to animate it scrolling up or popping: `initial={{ y: 10, opacity: 0 }} animate={{ y: 0, opacity: 1 }}` on key change.
- **Why this works**: Animating the number change draws the eye and gives weight to the achievement.
- **Confidence**: High

### [🟡] LevelUpOverlay Number Animation is Linear
- **What happens now**: The giant "NEW LEVEL" number spins continuously with a linear easing `ease: 'linear'`, which feels very mechanical and flat for a celebration screen.
- **Why it feels off**: Linear easing. Organic motion should rarely be linear unless simulating machinery.
- **Location**: `src/app/components/ui/system/LevelUpOverlay.tsx:56`
- **Proposed change**: `ease: 'linear' → ease: 'easeInOut'` or use a custom spring for the initial appearance of the decoration ring, then a very slow continuous rotation.
- **Why this works**: Non-linear easing adds an organic, magical feel to the leveling ritual.
- **Confidence**: Medium

### [🟢] LevelUpOverlay Transition Feels Abrupt on Exit
- **What happens now**: The LevelUp overlay disappears by scaling up to 1.2 and fading out over 0.4s, which works but feels a bit detached from the underlying game.
- **Why it feels off**: Missing follow-through. The overlay just vanishes rather than settling back into the UI.
- **Location**: `src/app/components/ui/system/LevelUpOverlay.tsx:49`
- **Proposed change**: `exit={{ scale: 1.2, opacity: 0 }}` → `exit={{ scale: 0.8, opacity: 0, filter: 'blur(10px)' }}` to make it feel like the energy is being absorbed back into the cards.
- **Why this works**: Shrinking down implies the power was transferred to the player/cards rather than just evaporating away.
- **Confidence**: Low

### [🟡] DynamicText Size Adjustments Pop Instantly
- **What happens now**: In `DynamicText`, when `useTextFit` recalculates the font size, the new size pops in immediately. While opacity fades in, the size change itself has no transition if it happens while visible.
- **Why it feels off**: Abruptness. It feels jumpy if a container resizes and text suddenly snaps to a new scale.
- **Location**: `src/app/components/ui/text/DynamicText.tsx:71`
- **Proposed change**: Add a very quick transition to font size if feasible, or rely entirely on the opacity fade. `transition: 'opacity 0.2s, font-size 0.1s ease-out'`.
- **Why this works**: Easing the text size prevents jarring layout shifts.
- **Confidence**: Medium

### [🟡] RewardCinematicOverlay Counter Lacks Roll-up
- **What happens now**: The XP and Resonance numbers appear instantly (`+{result.xp}` and `+{result.resonance}%`) when their respective cards animate in.
- **Why it feels off**: Missing anticipation/staging. Numbers that represent accumulated rewards feel better when they count up quickly from zero.
- **Location**: `src/app/components/ui/visual/RewardCinematicOverlay.tsx:70`
- **Proposed change**: Use a motion value counter or `useSpring` to tween the number from 0 to the target value over `1000ms`.
- **Why this works**: A fast count-up creates suspense and emphasizes the exact amount being awarded.
- **Confidence**: High

Tomorrow's focus: Card interactions
