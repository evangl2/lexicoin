Focus: Ambient/idle motion | Found: 4 items (🔴0 🟡2 🟢2 ⚪0) | Proposals: 4 | Applied: 0

### [🟡] LevelUpOverlay radial background pulse feels default
- **What happens now**: The central background light behind the level up number uses Tailwind's default `animate-pulse`, creating a harsh, linear on-off flashing that feels like a generic HTML element loading.
- **Why it feels off**: Easing is linear. Ambient light should breathe organically, not blink mechanically.
- **Location**: `src/app/components/ui/system/LevelUpOverlay.tsx:47`
- **Proposed change**: Remove `animate-pulse` and convert the `div` to a `motion.div` with `animate={{ opacity: [0.1, 0.4, 0.1] }}` and `transition={{ duration: 4, repeat: Infinity, ease: "easeInOut" }}`.
- **Why this works**: A sine-wave easing curve (easeInOut) over a longer duration mimics a steady, magical breath, creating a much more atmospheric and premium feel.
- **Confidence**: High

### [🟢] RewardCinematicOverlay Sparkles icon uses default pulse
- **What happens now**: The Sparkles icon in the intro sequence blinks linearly via `animate-pulse`.
- **Why it feels off**: Magic/energy indicators should have varied, non-linear brightness changes (secondary motion / easing).
- **Location**: `src/app/components/ui/visual/RewardCinematicOverlay.tsx:57`
- **Proposed change**: Remove `animate-pulse` and wrap it in a `motion.div` with `animate={{ scale: [1, 1.1, 1], opacity: [0.6, 1, 0.6] }}` and `transition={{ duration: 2, repeat: Infinity, ease: "easeInOut" }}`.
- **Why this works**: Adding a slight scale shift along with the opacity change makes the sparkle feel like it's physically glowing and expanding.
- **Confidence**: High

### [🟡] RewardCinematicOverlay Echo Reveal background uses default pulse
- **What happens now**: The golden blur behind the new Echo card flashes abruptly due to `animate-pulse`.
- **Why it feels off**: The default Tailwind pulse duration (2s) and cubic-bezier is too fast and sharp for a grand, ambient reveal, making it look like an error state rather than a magical aura.
- **Location**: `src/app/components/ui/visual/RewardCinematicOverlay.tsx:111`
- **Proposed change**: Replace `animate-pulse` with Framer Motion `animate={{ opacity: [0.2, 0.5, 0.2], scale: [1.4, 1.6, 1.4] }}` and `transition={{ duration: 5, repeat: Infinity, ease: "easeInOut" }}`.
- **Why this works**: A slow, 5-second cycle of expanding and contracting (breathing) gives the card a powerful, living presence.
- **Confidence**: High

### [🟢] SynthesisCircle decorative dashed ring is completely linear
- **What happens now**: The dashed ring in the Synthesis circle rotates at a constant speed using `animate-[spin_60s_linear_infinite]`.
- **Why it feels off**: While mechanical spinning is fine, it lacks any organic secondary motion to sell the idea of a magical, unstable alchemy process.
- **Location**: `src/app/components/ui/visual/SynthesisCircle.tsx:206`
- **Proposed change**: Keep the spin but convert to `motion.div` and add a very subtle breathing scale: `animate={{ rotate: 360, scale: [0.98, 1.02, 0.98] }}` with `transition={{ rotate: { duration: 60, repeat: Infinity, ease: "linear" }, scale: { duration: 8, repeat: Infinity, ease: "easeInOut" } }}`.
- **Why this works**: Overlaying an organic breathing scale on top of a mechanical spin makes the circle feel like it contains volatile energy.
- **Confidence**: Medium

Tomorrow's focus: Audio-visual pairing
