Focus: Audio-visual pairing | Found: 4 items (🔴4 🟡0 🟢0 ⚪0) | Proposals: 4 | Applied: 0

### [🔴] Card pickup lacks audio anchor
- **What happens now**: When dragging a card, it visually scales up and casts a shadow, but there is no sound confirming the physical action of picking it up.
- **Why it feels off**: Missing audio anchor. A physical card lifting off a table requires a synchronized sound (like a paper slide or soft snap) to sell the weight.
- **Location**: `src/app/hooks/useCardDrag.ts:104`
- **Proposed change**: Add `playSFX('card_pickup')` right after the dragging state begins and styles are updated.
- **Why this works**: Synchronizing a sharp "pickup" sound precisely on the frame the scale animation starts grounds the interaction in physical reality.
- **Confidence**: High (Requires missing asset: `card_pickup` sound file)

### [🔴] Card placement lacks audio anchor
- **What happens now**: When releasing a card, it visually snaps to the grid and triggers a visual overshoot, but there is no sound.
- **Why it feels off**: Missing audio anchor. The impact of a card landing on a surface feels hollow without an accompanying "thud" or "snap" to confirm the action.
- **Location**: `src/app/hooks/useCardDrag.ts:166`
- **Proposed change**: Add `playSFX('card_drop')` when the drag ends and the card is released to snap back.
- **Why this works**: A placement sound on release acts as the final resolution to the drag interaction, providing closure.
- **Confidence**: High (Requires missing asset: `card_drop` sound file)

### [🔴] Card flip lacks audio anchor
- **What happens now**: Right-clicking a card causes it to visually turn over with a spring animation, but it happens in silence.
- **Why it feels off**: Missing audio anchor. Flipping a stiff card requires mechanical effort, and silence makes it feel weightless and digital.
- **Location**: `src/app/components/ui/card/Card.tsx:393`
- **Proposed change**: Add `playSFX('card_flip')` inside the `onContextMenu` handler right before `setIsFlipped`.
- **Why this works**: A paper-whoosh or stiff-crease sound matching the flip duration gives the motion physical texture.
- **Confidence**: High (Requires missing asset: `card_flip` sound file)

### [🔴] Card expand/zoom lacks audio anchor
- **What happens now**: Clicking a card to zoom in rapidly scales it toward the camera without any audio feedback.
- **Why it feels off**: Missing audio anchor. Such a dramatic visual shift (bringing an item directly into focus) needs an auditory signal to mark the state change.
- **Location**: `src/app/components/ui/card/Card.tsx:386`
- **Proposed change**: Add `playSFX('card_expand')` inside the `onClick` handler when `setIsExpanded(!isExpanded)` is called to expand the card.
- **Why this works**: A soft "whoosh" or "focus" tone that accompanies the scale-up signals to the user that they are entering a detailed inspection state.
- **Confidence**: High (Requires missing asset: `card_expand` sound file)

Tomorrow's focus: Canvas camera feel
