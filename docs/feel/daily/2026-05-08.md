Focus: Audio-visual pairing | Found: 4 items (🔴2 🟡2 🟢0 ⚪0) | Proposals: 4 | Applied: 0

### [🔴] Card interactions lack audio anchors
- **What happens now**: When a user picks up, drops, or flips a card, the visual changes (scale up, shadow increase, flip animation) but there is no sound effect to anchor the interaction. TTS plays on pick up and click, but physical manipulation feels weightless.
- **Why it feels off**: Missing audio anchor. Physical interactions require corresponding audio feedback to feel tactile and responsive.
- **Location**: `src/app/hooks/useCardDrag.ts:108` and `src/app/components/ui/card/Card.tsx:385`
- **Proposed change**: Add `playSFX('card_pickup')` when `useDrag` starts (`first: true`), `playSFX('card_drop')` when it ends (`last: true`), and `playSFX('card_flip')` on right-click/flip.
- **Why this works**: Audio anchors confirm the physical action, adding weight and reality to the digital cards.
- **Confidence**: High

### [🔴] LevelUpOverlay misses triumphant audio cue
- **What happens now**: The level up screen appears with a grand animation (scale, opacity, rotation), but happens in complete silence.
- **Why it feels off**: Missing audio anchor. A major milestone like leveling up requires a celebratory sound to match the visual grandeur.
- **Location**: `src/app/components/ui/system/LevelUpOverlay.tsx:28`
- **Proposed change**: Trigger `playSFX('level_up_fanfare')` when the `LEVEL_UP` message is received and the overlay mounts.
- **Why this works**: Audio-visual pairing on success states is crucial for dopamine release and feeling of reward.
- **Confidence**: High

### [🟡] RewardCinematicOverlay lacks sequence audio
- **What happens now**: The cinematic sequence steps through 'intro', 'tally', and 'echo' stages visually, but without any audio cues to emphasize the transitions or the final reveal.
- **Why it feels off**: Missing audio anchor. The visual anticipation (staged reveals) is not supported by auditory build-up or climax.
- **Location**: `src/app/components/ui/visual/RewardCinematicOverlay.tsx:23`
- **Proposed change**: Add sequence sounds: `playSFX('reward_intro')` on mount, `playSFX('reward_tally')` at `tallyTimer`, and `playSFX('reward_reveal')` at `echoTimer`.
- **Why this works**: Synchronizing sound with visual stages enhances staging and makes the final reveal feel impactful.
- **Confidence**: High

### [🟡] SynthesisCircle completion is silent
- **What happens now**: When synthesis succeeds, the state changes to 'success' and the `synthCard` is generated visually, but there is no sound confirming the magical combination.
- **Why it feels off**: Missing audio anchor. A magical crafting process ending without a "poof" or "ding" feels anticlimactic and lacks follow-through.
- **Location**: `src/app/components/ui/visual/SynthesisCircle.tsx:64`
- **Proposed change**: Call `playSFX('synthesis_success')` inside the `useEffect` when `synthState === 'success'`.
- **Why this works**: An audio cue provides definitive closure and confirmation of the success state.
- **Confidence**: High

Tomorrow's focus: Canvas camera feel
