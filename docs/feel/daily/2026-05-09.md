### [🟡] Zoom inertia uses mechanical per-frame deceleration
- **What happens now**: When you stop scrolling the wheel or trackpad, the zoom momentum bleeds off mechanically per-frame, feeling like stepping on brakes rather than a smooth fluid settle.
- **Why it feels off**: Easing is linear/mechanical instead of organic.
- **Location**: `src/config/physics.ts:7` (`ZOOM_FRICTION`) and `src/app/components/ui/canvas/Canvas.tsx:184`
- **Proposed change**: Change `ZOOM_FRICTION: 0.72 → 0.85` to extend the glide, or ideally switch the `runZoomInertia` loop to use a proper spring physics simulation.
- **Why this works**: A longer decay or spring-based follow-through provides a natural, smooth settle that feels less jarring.
- **Confidence**: High

### [🟡] Camera `flyTo` uses overly loose spring
- **What happens now**: Programmatic camera moves (like focusing on a target) feel slightly floaty and take a bit too long to settle exactly onto the target.
- **Why it feels off**: Weak follow-through/easing. Spring damping is too low for a UI camera.
- **Location**: `src/app/hooks/useCanvasCamera.ts:28`
- **Proposed change**: `stiffness: 100, damping: 20` → `stiffness: 150, damping: 25`.
- **Why this works**: Increasing stiffness and damping removes the floaty feeling, making the camera move snappy and authoritative.
- **Confidence**: High

### [🟡] Pan boundary resistance feels rigid
- **What happens now**: When dragging past the canvas edge, the camera meets resistance that ramps up too quickly and linearly, feeling like hitting a thick wall rather than an elastic band.
- **Why it feels off**: Easing is mechanical and lacks the elastic squash & stretch feel.
- **Location**: `src/config/camera.ts:20` (`RESISTANCE_STEEPNESS: 1.5`, `RESISTANCE_MULTIPLIER: 4.0`)
- **Proposed change**: `RESISTANCE_STEEPNESS: 1.5 → 2.5`, `RESISTANCE_MULTIPLIER: 4.0 → 6.0`.
- **Why this works**: A steeper curve allows more initial give before drastically increasing resistance, simulating the squash and stretch of an elastic band snapping back.
- **Confidence**: Medium

### [🟡] Pixi camera `lookAt` uses standard duration regardless of distance
- **What happens now**: The GSAP `lookAt` animation takes exactly 0.8s whether moving 10 pixels or 8000 pixels. Short moves feel incredibly sluggish, while long moves feel rushed.
- **Why it feels off**: Non-dynamic duration breaks the responsiveness of the interaction.
- **Location**: `src/pixi/systems/CameraSystem.ts:114` and `src/config/camera.ts:25` (`LOOKAT_DURATION: 0.8`)
- **Proposed change**: Calculate duration dynamically based on distance: e.g. `duration: Math.min(1.2, Math.max(0.4, distance / 3000))` instead of fixed `0.8`.
- **Why this works**: Dynamic durations ensure consistent velocity, so small nudges feel snappy and cross-world jumps don't induce motion sickness.
- **Confidence**: High

### [🟢] Pixi Camera Viewport edge bounce is generic
- **What happens now**: Viewport edge bouncing uses a standard `easeOutBack` with a 400ms time, making it feel like a generic UI pop rather than heavy camera equipment hitting an edge.
- **Why it feels off**: Follow-through lacks weight.
- **Location**: `src/pixi/systems/CameraSystem.ts:51` and `src/config/camera.ts:24` (`BOUNCE_TIME: 400`)
- **Proposed change**: `BOUNCE_TIME: 400 → 600`, `ease: 'easeOutBack' → 'easeOutElastic(1, 0.7)'`.
- **Why this works**: A slightly longer, more elastic bounce adds weight to the world, suggesting you hit a physical boundary.
- **Confidence**: Medium
