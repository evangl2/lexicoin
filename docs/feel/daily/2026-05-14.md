### [🟡] LevelUpOverlay radial background pulse feels default
- **What happens now**: The background radial gradient uses the default tailwind pulse, feeling mechanical and evenly paced.
- **Why it feels off**: Linear/predictable easing (mechanical intent instead of organic/magical).
- **Location**: `src/app/components/ui/system/LevelUpOverlay.tsx:47`
- **Proposed change**: Replace `animate-pulse` with a custom Framer Motion animation: `animate={{ opacity: [0.3, 0.8, 0.3], scale: [1, 1.05, 1] }}` with `transition={{ duration: 4, ease: "easeInOut", repeat: Infinity }}`.
- **Why this works**: Slower, smoothed easing creates a gentle "breathing" ambient motion suitable for a magical overlay.
- **Confidence**: High

### [🟢] RewardCinematicOverlay Sparkles icon uses default pulse
- **What happens now**: The Sparkles icon flashes rigidly using the standard CSS pulse.
- **Why it feels off**: Lacks organic secondary motion and squash & stretch for a magical element.
- **Location**: `src/app/components/ui/visual/RewardCinematicOverlay.tsx:57`
- **Proposed change**: Remove `animate-pulse`. Wrap in `motion.div` and animate: `animate={{ rotate: [0, 15, -5, 0], scale: [1, 1.2, 0.9, 1] }}` with `transition={{ duration: 2, repeat: Infinity, ease: "easeInOut" }}`.
- **Why this works**: Combining rotation and scale creates a twinkling, alive feeling rather than a mechanical blink.
- **Confidence**: High

### [🟡] RewardCinematicOverlay Echo Reveal background uses default pulse
- **What happens now**: The blurred background behind the revealed card pulses intensely and rigidly.
- **Why it feels off**: Default pulse is too abrupt for a grand, awe-inspiring reveal, violating ambient breathing principles.
- **Location**: `src/app/components/ui/visual/RewardCinematicOverlay.tsx:111`
- **Proposed change**: Remove `animate-pulse`. Use Framer Motion on the div: `animate={{ opacity: [0.5, 0.8, 0.5], scale: [1.5, 1.6, 1.5] }}` with `transition={{ duration: 3, repeat: Infinity, ease: "easeInOut" }}`.
- **Why this works**: A slower, easing-driven breathing effect makes the manifestation feel powerful and stable.
- **Confidence**: High

### [🟢] SynthesisCircle decorative dashed ring is completely linear
- **What happens now**: The dashed ring spins at a perfectly constant speed (`spin_60s_linear_infinite`).
- **Why it feels off**: Easing is linear; while it's a mechanical ring, it lacks ambient life or dynamic tension.
- **Location**: `src/app/components/ui/visual/SynthesisCircle.tsx:206`
- **Proposed change**: Keep the spin but add a breathing scale via Framer Motion: `animate={{ scale: [1, 1.02, 1] }}` with `transition={{ duration: 8, repeat: Infinity, ease: "easeInOut" }}` to the ring.
- **Why this works**: Adding a subtle, very slow secondary scale motion gives the circle an organic pulse.
- **Confidence**: Medium
