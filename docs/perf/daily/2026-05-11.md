Focus: React rendering | Found: 3 issues (🔴0 🟡0 🟢3 ⚪0) | Auto-fixed: 3 | Awaiting approval: 0

### [🟢] O(N) Array Allocation in SceneManager
- **Symptom**: Slight overhead on layout renders when navigating around the app.
- **Location**: `src/app/components/ui/shell/SceneManager.tsx:64`
- **Root cause**: The `useShallow` wrapper was used directly on an array retrieved from the store (`s.activeGrimoires`), resulting in an O(N) shallow equality check instead of a simple O(1) identity check.
- **Suggested fix**: Removed the unnecessary `useShallow` wrapper.
- **Expected gain**: Minor performance bump; reduces linear time check overhead on mount or store updates.
- **Fix risk**: Low

### [🟢] Unnecessary re-renders from Zustand player selector
- **Symptom**: Potentially unnecessary component re-renders when the `player` state updates.
- **Location**: `src/app/components/ui/grimoire/GrimoireSlot.tsx:35`
- **Root cause**: The `player` store object was selected instead of selecting only `player.settings`. This means any update to `player` (e.g. `stamina`) will re-render `GrimoireSlot`.
- **Suggested fix**: Selected only `player?.settings`.
- **Expected gain**: Saves some React render cycles.
- **Fix risk**: Low

### [🟢] Unnecessary re-renders from Zustand player selector
- **Symptom**: Unnecessary component re-renders when other `player` subfields change.
- **Location**: `src/app/components/system/DevConsole.tsx:123`
- **Root cause**: The `player` store object was selected instead of selecting specific properties (`stamina`, `maxStamina`, `settings.learningLang`, `languageProgress`).
- **Suggested fix**: Used granular selectors to pick only necessary fields.
- **Expected gain**: Saves some React render cycles.
- **Fix risk**: Low

Tomorrow's focus: Canvas rendering
