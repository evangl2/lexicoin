Focus: Build | Found: 2 issues (🔴0 🟡0 🟢2 ⚪0) | Auto-fixed: 2 | Awaiting approval: 0

### [🟢] B-012: Vite Chunking Warning from Mixed Static/Dynamic Imports
- **Symptom**: Build logs show a Rollup warning about `MessageBus.ts` being imported both statically and dynamically. This prevents Rollup from properly chunking the module, leading to unnecessary async execution overhead and a bloated main app bundle.
- **Location**: `src/core/store/slices/createGrimoireSlice.ts:143`, `163`
- **Root cause**: `MessageBus.ts` is imported statically across the app, but inside `createGrimoireSlice.ts` it is lazily imported using dynamic `import()`. Rollup cannot resolve this cleanly into an isolated chunk, so it merges it while retaining the async wrapper, defeating the purpose of the lazy import.
- **Suggested fix**: Convert the dynamic `import('../../protocol/MessageBus')` calls inside `unfillGrimoireSlot` and `clearAllGrimoires` to standard static `import { messageBus } from '@core/protocol/MessageBus'` at the top level of the file.
- **Expected gain**: Cleaner bundle, no Rollup warnings, and avoiding tiny async delays in synchronous store actions.
- **Fix risk**: Low (Auto-fixed)

### [🟢] B-013: Missing pixi.js chunking leads to massive main bundle
- **Symptom**: The main `index.js` app chunk is larger than 600KB minified. Users must download the entire Pixi.js engine even if they initially visit a route without the canvas.
- **Location**: `vite.config.ts:60`
- **Root cause**: The `manualChunks` function correctly splits out React, Supabase, and Dexie, but misses `pixi.js` and `@pixi/*` dependencies. As a result, the canvas engine falls back into the main chunk, heavily bloating the initial page load for all users.
- **Suggested fix**: Add a condition in `manualChunks` to return `'vendor-pixi'` for IDs containing `pixi.js` or `@pixi/`.
- **Expected gain**: The main application chunk drops to ~360KB, meaning faster initial parses, while Pixi.js sits in a dedicated cached ~570KB chunk.
- **Fix risk**: Low (Auto-fixed)

Tomorrow's focus: React rendering
