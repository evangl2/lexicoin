Focus: React rendering | Found: 4 issues (🔴0 🟡0 🟢4 ⚪0) | Auto-fixed: 4 | Awaiting approval: 0

### [🟢] Unnecessary re-renders from DevConsole player selector
- **Symptom**: The hidden DevConsole re-renders anytime any player property changes (like stamina or xp), wasting CPU cycles.
- **Location**: `src/app/components/system/DevConsole.tsx:119`
- **Root cause**: Selecting the entire `player` object means the component re-renders if *anything* inside the player changes.
- **Suggested fix**: Use `useShallow` when selecting objects from the store if we need the whole object, or select specific properties if we don't.
- **Expected gain**: Saves wasted render cycles when the console is hidden but game state is active.
- **Fix risk**: Low

### [🟢] Unnecessary re-renders from GrimoireSlot player selector
- **Symptom**: Grimoire slots might slightly lag the UI when player stamina or XP changes in the background.
- **Location**: `src/app/components/ui/grimoire/GrimoireSlot.tsx:35`
- **Root cause**: The component selects the entire `player` object just to read `learningLang` and `interfaceLang`.
- **Suggested fix**: Select exactly what is needed: `useGameStore(s => s.player.settings.learningLang)` and `interfaceLang`.
- **Expected gain**: Prevents grimoire slots from re-rendering on unrelated player state updates (like XP gains).
- **Fix risk**: Low

### [🟢] Unnecessary function recreations in useEchoSystem
- **Symptom**: Echo extraction hook could cause extra re-renders or function recreation if unrelated player settings change.
- **Location**: `src/app/hooks/useEchoSystem.ts:12`
- **Root cause**: The hook selects `player.settings` eagerly but only uses it inside the `extractEcho` callback.
- **Suggested fix**: Remove the eager selector and use `useGameStore.getState().player?.settings` inside the callback.
- **Expected gain**: Less memory allocation and component updates when player settings change.
- **Fix risk**: Low

### [🟢] Unnecessary function recreations in useGrimoireReward
- **Symptom**: Reward claiming hook could cause extra function recreation when unrelated player settings change.
- **Location**: `src/app/hooks/useGrimoireReward.ts:22`
- **Root cause**: Selects `player.settings` eagerly but only uses it inside the `claim` callback.
- **Suggested fix**: Remove eager selector and use `useGameStore.getState().player?.settings` inside the callback.
- **Expected gain**: Slightly better React hook performance.
- **Fix risk**: Low

Tomorrow's focus: Canvas rendering
