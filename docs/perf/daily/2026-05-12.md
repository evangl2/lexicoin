Focus: Canvas rendering | Found: 1 issues (🔴0 🟡0 🟢1 ⚪0) | Auto-fixed: 1 | Awaiting approval: 0

### [🟢] Unnecessary RequestAnimationFrame Opacity Toggle
- **Symptom**: Potential micro-stutter or battery drain when cards finish zooming/scaling.
- **Location**: `src/app/hooks/useCardAnimation.ts:266`
- **Root cause**: The codebase was manually forcing GPU layer re-rasterization by toggling `opacity` between `0.9999` and `1` using nested `requestAnimationFrame` calls. Modern browsers handle layer demotion naturally without needing this manual paint-thrashing hack.
- **Suggested fix**: Removed the double `requestAnimationFrame` block inside `forceRerasterize`, relying strictly on `applyDemoted()`.
- **Expected gain**: Removes two forced paint flashes and reduces main-thread layout thrashing when card animations settle.
- **Fix risk**: Low

Tomorrow's focus: Animations
