Focus: Canvas rendering | Found: 1 issues (🔴0 🟡0 🟢1 ⚪0) | Auto-fixed: 1 | Awaiting approval: 0

### [🟢] Remove nested rAF opacity toggling in Card Animation
- **Symptom**: Slight stutter or frame drop when a card finishes scaling up/down.
- **Location**: `src/app/hooks/useCardAnimation.ts:258`
- **Root cause**: The code uses a nested `requestAnimationFrame` to momentarily set opacity to 0.9999 and back to 1. This was historically used to force GPU re-rasterization, but modern browsers handle this automatically. The workaround just causes unnecessary layout and paint spikes.
- **Suggested fix**: Remove the nested `requestAnimationFrame` block inside `forceRerasterize`.
- **Expected gain**: Saves layout/paint time on frames where card animations settle.
- **Fix risk**: Low

Tomorrow's focus: Animations
