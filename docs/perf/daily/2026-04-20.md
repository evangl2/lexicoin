# Performance Audit — 2026-04-20 (Monday)

**Focus: React rendering | Found: 1 issue (🔴0 🟡0 🟢0 ⚪1) | Auto-fixed: 1 | Awaiting approval: 0**

---

## Context

Today is Monday → React rendering domain: unnecessary re-renders, missing memo/useMemo/useCallback, Zustand selector granularity.

Scanned `src/app/components/` and `src/app/hooks/` for React rendering and Zustand optimization patterns. The codebase is generally very healthy: most array selectors (like `notifications` and `libraryGrimoires`) correctly return existing store references, relying on Zustand's optimal strict equality check.

One minor issue was found related to selector granularity in the Progression HUD.

---

## Issues

### [⚪] P-001: ProgressionHUD uses verbose primitive extraction over grouped selector

- **Symptom**: No user-visible lag. The code is just a bit verbose and triggers multiple individual selector evaluations on every state change.
- **Location**: `src/app/components/ui/shell/ProgressionHUD.tsx:22`
- **Root cause**: `ProgressionHUD` extracts `level`, `xp`, `xpToNextLevel`, `stamina`, and `maxStamina` via five separate `useGameStore` calls. While this avoids infinite re-renders by returning primitives, it ignores the existing `selectLanguageLevelInfo` selector provided in `@store/selectors.ts`. Since `selectLanguageLevelInfo` generates a new object, the correct pattern would be to wrap it in `useShallow` so the component only re-renders when the grouped data actually changes, yielding cleaner code.
- **Suggested fix**: Refactor to `const progressInfo = useGameStore(useShallow(s => selectLanguageLevelInfo(s, learningLang)))`.
- **Expected gain**: Cleaner code and slightly fewer hook evaluations per render cycle. No perceivable frame rate difference.
- **Fix risk**: Low. Auto-fix applied.

---

## Auto-applied changes

- P-001: Auto-fixed primitive extraction in `ProgressionHUD.tsx` using `useShallow`

---

Tomorrow's focus: Canvas rendering
