Focus: recent | Found: 4 params (🔴0 🟡2 🟢2 ⚪0) | Auto-extracted: 2 | Proposed: 2 | Registry size: 8

## Auto-Extracted — P2 Hygiene

### [🟢] SYNTHESIS_LONG_STATE_DELAY_MS
- **Current**: `src/app/hooks/useSynthesis.ts:151 → setTimeout(..., 15000)`
- **Meaning**: The delay before showing the "taking a while" / processing-long state.
- **Why extract**: Good hygiene to have timing constants documented.
- **Proposed home**: `src/config/timing.ts → SYNTHESIS_LONG_STATE_DELAY_MS`
- **Risk**: Low

### [🟢] HUD_PROGRESS_BAR_SPRING
- **Current**: `src/app/components/ui/shell/ProgressionHUD.tsx:64, 83 → { stiffness: 50, damping: 20 }`
- **Meaning**: The spring animation physics for the progression and stamina bars.
- **Why extract**: Tuning spring stiffness to make the bars fill smoothly instead of snapping.
- **Proposed home**: `src/config/physics.ts → HUD_PROGRESS_BAR_SPRING`
- **Risk**: Low

---

## Proposed — P1 Design-Relevant

### [🟡] SYNTHESIS_TIMEOUT_MS
- **Current**: `src/app/hooks/useSynthesis.ts:156 → setTimeout(..., 90_000)`
- **Meaning**: Hard fallback timeout for synthesis operations (90 seconds).
- **Why extract**: Needs tuning if AI response times vary significantly.
- **Proposed home**: `src/config/timing.ts → SYNTHESIS_TIMEOUT_MS`
- **Proposed DevPanel control**: `slider: 30_000–120_000ms, step 5000` — live
- **Risk**: Low

```tsx
// DevPanel proposal:
<div className="control-group">
  <label>Synthesis Timeout (ms)</label>
  <input
    type="range"
    min="30000"
    max="120000"
    step="5000"
    onChange={(e) => updateConfig('SYNTHESIS_TIMEOUT_MS', Number(e.target.value))}
  />
</div>
```

### [🟡] SYNTHESIS_POLL_DELAYS
- **Current**: `src/app/hooks/useSynthesis.ts:74 → const delays = [25_000, 25_000, 50_000]`
- **Meaning**: Delays between consecutive polling attempts for sense visuals.
- **Why extract**: AI stability affects when visuals are ready; tuning polling saves requests but shouldn't delay UX.
- **Proposed home**: `src/config/timing.ts → SYNTHESIS_POLL_DELAYS`
- **Proposed DevPanel control**: Inputs for each delay slot — live
- **Risk**: Medium

```tsx
// DevPanel proposal:
<div className="control-group">
  <label>Poll Delays [ms, ms, ms]</label>
  <input
    type="text"
    placeholder="25000, 25000, 50000"
    onChange={(e) => updateConfig('SYNTHESIS_POLL_DELAYS', e.target.value.split(',').map(Number))}
  />
</div>
```
