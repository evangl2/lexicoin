Focus: recent | Found: 15 params (🔴0 🟡9 🟢6 ⚪0) | Auto-extracted: 3 | Proposed: 8 | Registry size: 13

Files scanned this session: `Card.tsx`, `useTieredAutoType.ts`, `LevelUpOverlay.tsx`, `RewardCinematicOverlay.tsx`, `useSynthesis.ts`, `XPRegistry.ts`

---

## Auto-Extracted & Wired (Hygiene P2)

### [🟢] CARD_ANIMATION_LOCKOUT_MS
- **Current**: `src/config/timing.ts`
- **Meaning**: How long after a merge/split event the card ignores new hover/interaction (prevents visual glitch during animation).
- **Why extract**: Found in previous session, now auto-extracted.
- **Wired in**: `src/app/components/ui/card/Card.tsx`
- **Risk**: Low

### [🟢] TEXT_FIT_DEBOUNCE_MS
- **Current**: `src/config/timing.ts`
- **Meaning**: Delay for debouncing resize observer in the tiered auto-type hook.
- **Why extract**: Magic number for UI layout performance that might need tuning.
- **Wired in**: `src/app/hooks/useTieredAutoType.ts`
- **Risk**: Low

### [🟢] LEVEL_UP_DURATION_MS
- **Current**: `src/config/timing.ts`
- **Meaning**: How long the level up overlay remains visible before auto-dismissing.
- **Why extract**: Pure timing feel logic, easily tunable.
- **Wired in**: `src/app/components/ui/system/LevelUpOverlay.tsx`
- **Risk**: Low

### [🟢] REWARD_CINEMATIC (Wired)
- **Current**: `src/config/timing.ts`
- **Meaning**: Timings for intro, tally, and echo states.
- **Why extract**: Was already in config but not wired.
- **Wired in**: `src/app/components/ui/visual/RewardCinematicOverlay.tsx`
- **Risk**: Low

### [🟢] SYNTHESIS_POLL_DELAYS_MS / SYNTHESIS_HARD_TIMEOUT_MS (Wired)
- **Current**: `src/config/timing.ts`
- **Meaning**: Wait intervals for polling and the hard timeout for synthesis edge function.
- **Why extract**: Was already in config but not wired.
- **Wired in**: `src/app/hooks/useSynthesis.ts`
- **Risk**: Low

### [🟢] XP_FACTORS / XP_CONSTRUCTION_FIXED (Wired)
- **Current**: `src/config/balance.ts`
- **Meaning**: XP values for duplicate fractions, evolution multipliers, and fixed construction XP.
- **Why extract**: Was already in config but not wired.
- **Wired in**: `src/core/services/XPRegistry.ts`
- **Risk**: Low

---

## Proposed — P1 Design-Relevant

### [🟡] CARD_HOVER_SCALE / CARD_DRAG_SCALE
- **Current**: `Card.tsx:580 → scaleSpring.set(1.15)` (drag start), `Card.tsx:722 → scaleSpring.set(1.05)` (hover)
- **Meaning**: How much the card grows when hovered vs. dragged.
- **Why extract**: Two values that directly affect "does this feel satisfying to grab".
- **Proposed home**: `src/config/canvas.ts → CARD_SCALE = { HOVER: 1.05, DRAG: 1.15 } as const`
- **Proposed DevPanel control**: Two sliders (1.0–1.5, step 0.01). Live. State binding: global `useGameStore` slice or passed via Context. Needs "Reset to default" button.
  ```diff
  + <div className="system-panel-row">
  +   <label>Hover Scale</label>
  +   <input type="range" min="1.0" max="1.5" step="0.01" value={cardHoverScale} onChange={e => setCardHoverScale(e.target.value)} />
  +   <button onClick={resetCardHoverScale}>Reset</button>
  + </div>
  ```
- **Risk**: Medium

### [🟡] CARD_EXPANDED_VIEWPORT_FRACTION
- **Current**: `Card.tsx:362`
- **Meaning**: When a card is expanded (clicked), it fills 80% of the viewport's smaller dimension.
- **Why extract**: 80% is a design judgment. On mobile you'd want 90%.
- **Proposed home**: `src/config/canvas.ts → CARD_EXPANDED_VIEWPORT_FRACTION = 0.8`
- **Proposed DevPanel control**: Slider 0.4–1.0, step 0.05. Live. State binding: global `useGameStore` slice. Needs "Reset to default" button.
  ```diff
  + <div className="system-panel-row">
  +   <label>Expanded Viewport Fraction</label>
  +   <input type="range" min="0.4" max="1.0" step="0.05" value={viewportFraction} onChange={e => setViewportFraction(e.target.value)} />
  +   <button onClick={resetViewportFraction}>Reset</button>
  + </div>
  ```
- **Risk**: Low

### [🟡] GRID_OPACITY_TRANSFORMS
- **Current**: `DefaultGridSystem.tsx:16-17`
- **Meaning**: Opacity transforms across zoom levels for the visual background grids.
- **Why extract**: Affects visual noise and readability across zoom levels.
- **Proposed home**: `src/config/visual.ts → GRID_OPACITY = { ... } as const`
- **Proposed DevPanel control**: Individual sliders for each opacity endpoint. Live. State binding: global `useGameStore` slice. Needs "Reset to default" button.
  ```diff
  + <div className="system-panel-row">
  +   <label>Small Grid Max Opacity</label>
  +   <input type="range" min="0.1" max="1.0" step="0.1" value={smallGridOpacityMax} onChange={e => setSmallGridOpacityMax(e.target.value)} />
  +   <button onClick={resetSmallGridOpacityMax}>Reset</button>
  + </div>
  ```
- **Risk**: Medium

### [🟡] TEXT_FIT_RATIO_THRESHOLDS
- **Current**: `useTieredAutoType.ts` -> 1.05 (overflow), 1.3 (jump 2), 1.8 (jump 3), 0.40 (underflow)
- **Meaning**: Ratios at which the text will downgrade/upgrade its visual tier to prevent overflow.
- **Why extract**: Controls layout stability and text readability.
- **Proposed home**: `src/config/visual.ts → TEXT_FIT_THRESHOLDS = { OVERFLOW: 1.05, JUMP2: 1.3, JUMP3: 1.8, UNDERFLOW: 0.40 } as const`
- **Proposed DevPanel control**: Sliders for thresholds. Live. State binding: global `useGameStore` slice. Needs "Reset to default" button.
  ```diff
  + <div className="system-panel-row">
  +   <label>Text Overflow Ratio</label>
  +   <input type="range" min="1.0" max="1.5" step="0.01" value={textFitOverflow} onChange={e => setTextFitOverflow(e.target.value)} />
  +   <button onClick={resetTextFitOverflow}>Reset</button>
  + </div>
  ```
- **Risk**: Medium

---

## Closing Notes
- Registry at 13 entries.
- Wire-up pass completed for timing and balance parameters missed in previous extraction passes.
- Several visual and layout physics parameters are queued for the next extraction pass.
