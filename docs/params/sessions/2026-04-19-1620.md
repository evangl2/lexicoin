# Parameter Session — 2026-04-19 16:20

**Focus: recent** (files changed in last 7 days)  
**Found: 26 params (🔴5 🟡11 🟢10 ⚪some skipped) | Auto-extracted: 3 | Proposed: 23 | Registry size: 4**

Files scanned: `useGrimoireSummoning.ts`, `useEchoSystem.ts`, `useGrimoireReward.ts`, `createGrimoireSlice.ts`, `createProgressionSlice.ts`, `GrimoireSlotVisual.tsx`, `GrimoireSummoner.tsx`, `GrimoireOverlay.tsx`, `Grimoire.tsx`, `LibraryInterface.tsx`, `useGridSnap.ts`, `useViewportCulling.ts`, `Canvas.tsx`

---

## Auto-Extracted (3)

These were applied without value change — only relocated/named.

### [🟡] GRIMOIRE_DURATION_MS
- **Was**: `const GRIMOIRE_DURATION_MS = 60 * 60 * 1000;` local to `useGrimoireSummoning.ts:23`
- **Now**: `src/config/grimoireConfig.ts → GRIMOIRE_DURATION_MS`
- **Why extracted**: Grimoire lifespan is a core game design parameter — game designers may want to test 30-min vs 2-hour windows without touching hook logic.

### [🟡] GRIMOIRE_SLOT_COUNT.{MIN, MAX, DEFAULT}
- **Was**: `Math.max(3, Math.min(6, slotCount)) : 4` inline in `useGrimoireSummoning.ts:115-116`
- **Now**: `src/config/grimoireConfig.ts → GRIMOIRE_SLOT_COUNT = { MIN: 3, MAX: 6, DEFAULT: 4 }`
- **Why extracted**: Slot count range directly affects difficulty and evaluation variety. Both ends of the range and the fallback belong in config, not scattered inline.

### [🟡] Stamina check hardcode fix (`GrimoireSummoner.tsx:83`)
- **Was**: `stamina >= 60` hardcoded
- **Now**: `stamina >= STAMINA_CONFIG.COSTS.GENERATE_GRIMOIRE`
- **Why**: The cost was already in grimoireConfig.ts. The component had a stale copy that would silently diverge if the cost was ever changed — a latent bug. Value unchanged (still 60).

---

## Proposed — P0 Tuning-Critical

### [🔴] ZOOM_FRICTION
- **Current**: `Canvas.tsx:144 → const ZOOM_FRICTION = 0.72`
- **Meaning**: How quickly zoom inertia decelerates — higher = shorter glide, lower = longer, floatier zoom.
- **Why extract**: Zoom feel is the #1 thing playtesters will want to tweak. Currently buried inside a 350-line component.
- **Proposed home**: `src/config/physics.ts → ZOOM_FRICTION`
- **Proposed DevPanel control**: `slider: 0.5–0.95, step 0.01` — live, Zustand or local ref, no restart needed
- **Risk**: Low (pure rename)

### [🔴] ZOOM_MIN_VELOCITY
- **Current**: `Canvas.tsx:145 → const ZOOM_MIN_VELOCITY = 0.5`
- **Meaning**: Velocity below which inertia loop stops — prevents endless micro-drift.
- **Why extract**: Pairs with ZOOM_FRICTION; both must be tuned together for feel.
- **Proposed home**: `src/config/physics.ts → ZOOM_MIN_VELOCITY`
- **Proposed DevPanel control**: `slider: 0.1–2.0, step 0.05` — live
- **Risk**: Low

### [🔴] ZOOM_SENSITIVITY
- **Current**: `Canvas.tsx:175, 267 → Math.exp(-applied * 0.001)` (appears twice)
- **Meaning**: Converts accumulated wheel delta to zoom multiplier — smaller = slower zoom per scroll tick.
- **Why extract**: Mis-matched sensitivity is the most common "feels bad" complaint. Currently duplicated in two places.
- **Proposed home**: `src/config/physics.ts → ZOOM_SENSITIVITY` (0.001)
- **Proposed DevPanel control**: `slider: 0.0002–0.003, step 0.0001` — live
- **Risk**: Medium (duplicated inline — needs two edits; both must stay in sync)

### [🔴] SNAP_SPRING (stiffness / damping / mass)
- **Current**: `useGridSnap.ts:75 → { type: 'spring', stiffness: 400, damping: 35, mass: 0.8 }`
- **Meaning**: Spring physics for card snap animation — stiffness = snap speed, damping = oscillation, mass = weight feel.
- **Why extract**: Card snap is tactile feedback; playtesters will want bouncy vs crisp variants.
- **Proposed home**: `src/config/physics.ts → SNAP_SPRING = { stiffness: 400, damping: 35, mass: 0.8 } as const`
- **Proposed DevPanel control**: three sliders — stiffness: 100–800, damping: 10–80, mass: 0.1–2.0 — live
- **Risk**: Low

### [🔴] CEFR Level Thresholds
- **Current**: `useGrimoireSummoning.ts:83-86`
  ```ts
  levelInLang >= 20 ? 'C1' :
  levelInLang >= 15 ? 'B2' :
  levelInLang >= 10 ? 'B1' :
  levelInLang >= 5  ? 'A2' : 'A1'
  ```
- **Meaning**: Maps player language level to vocabulary difficulty tier for grimoire generation. Controls how hard the AI-generated words are.
- **Why extract**: This is the primary difficulty scaling mechanism. The thresholds determine how quickly players encounter harder vocabulary. Should live in `balance.ts` alongside `LEVEL_CEFR_DISTRIBUTION`.
- **Proposed home**: `src/config/balance.ts → GRIMOIRE_CEFR_LEVEL_THRESHOLDS = { A2: 5, B1: 10, B2: 15, C1: 20 } as const`
- **Proposed DevPanel control**: four number inputs, range 1–100 — requires store reload (affects next summon)
- **Risk**: Medium (business logic, affects AI prompt input)

---

## Proposed — P1 Design-Relevant

### [🟡] GRIMOIRE_DURATION_MS (already extracted — entry for reference)
- Extracted to `src/config/grimoireConfig.ts`. Default: `3_600_000` (1 hour).
- **Proposed DevPanel control**: `dropdown: [30min, 1h, 2h, 4h]` — affects next summon only
- **Risk**: Low

### [🟡] READY_FLASH_MS
- **Current**: `GrimoireSummoner.tsx:107 → setTimeout(..., 1200)`
- **Meaning**: How long the READY state (green glow + "Grimoire Manifested") is shown after summoning before reverting to IDLE.
- **Why extract**: Too short feels like a miss; too long feels sluggish. Tuning window for feel.
- **Proposed home**: `src/config/grimoireConfig.ts → SUMMONER_READY_FLASH_MS`
- **Proposed DevPanel control**: `slider: 400–3000ms, step 100` — live
- **Risk**: Low

### [🟡] ECHO_EXTRACTION_COOLDOWN_MS
- **Current**: `useEchoSystem.ts:68 → setTimeout(() => setIsExtracting(false), 1500)`
- **Meaning**: UI lockout between echo extractions — prevents double-tapping.
- **Why extract**: Should be long enough to feel deliberate but short enough not to annoy.
- **Proposed home**: `src/config/grimoireConfig.ts → ECHO_EXTRACTION_COOLDOWN_MS`
- **Proposed DevPanel control**: `slider: 500–3000ms, step 100` — live
- **Risk**: Low

### [🟡] GRIMOIRE_TIMER_WARNING_THRESHOLDS
- **Current**: `Grimoire.tsx:45-46`
  ```ts
  remaining > 20 * 60_000 ? '#22d3ee' :  // cyan
  remaining > 10 * 60_000 ? '#f97316' :  // orange
  '#ef4444'                               // red
  ```
- **Meaning**: When timer is >20 min → cyan, 10–20 min → orange, <10 min → red + pulsing. Controls player urgency signaling.
- **Why extract**: Warning thresholds depend on total grimoire duration. If duration changes, these breakpoints need updating together.
- **Proposed home**: `src/config/grimoireConfig.ts → GRIMOIRE_TIMER_WARN = { ORANGE_MS: 20 * 60_000, RED_MS: 10 * 60_000 } as const`
- **Proposed DevPanel control**: two sliders (as % of total duration), range 5%–80% — live
- **Risk**: Low

### [🟡] MAX_ZOOM / MIN_ZOOM
- **Current**: `Canvas.tsx:171, 175, 266, 267 → 2.0` (max zoom, appears 4 times); min is computed from world/screen ratio
- **Meaning**: Upper zoom limit. Lower limit is dynamic (screen/world ratio), but 0.08 is used as a floor.
- **Why extract**: Zoom range defines exploration depth. Too much zoom loses world context; too little makes cards unreadable.
- **Proposed home**: `src/config/physics.ts → ZOOM_MAX = 2.0`, `ZOOM_MIN_FLOOR = 0.08`
- **Proposed DevPanel control**: `slider: 1.5–4.0` for max, `slider: 0.03–0.15` for min floor — live
- **Risk**: Medium (appears 4 times — all need updating)

### [🟡] CANVAS_OVERSCROLL
- **Current**: `Canvas.tsx:107-108 → const OVERSCROLL_X = 300; const OVERSCROLL_Y = 150`
- **Meaning**: Extra pixels of pan allowed beyond the world edge — creates breathing room so the world doesn't feel like a wall.
- **Why extract**: Game feel during exploration. Asymmetric (X wider than Y) because cards are landscape-oriented.
- **Proposed home**: `src/config/physics.ts → CANVAS_OVERSCROLL = { X: 300, Y: 150 } as const`
- **Proposed DevPanel control**: two sliders, range 0–600px — live
- **Risk**: Low

### [🟡] VIEWPORT_CULL_MARGIN
- **Current**: `useViewportCulling.ts:17 → const CULL_MARGIN = 2500`
- **Meaning**: Pixels beyond viewport edge where cards are still mounted. Large value = more cards in DOM but no pop-in. Too large = frame drops.
- **Why extract**: A critical perf/quality tradeoff lever — device performance varies. The comment says it was already deliberately tuned once.
- **Proposed home**: `src/config/physics.ts → VIEWPORT_CULL_MARGIN`
- **Proposed DevPanel control**: `slider: 200–5000px, step 100` — live (triggers recull)
- **Risk**: Medium (performance sensitive)

### [🟡] CANVAS_PARALLAX_FACTOR
- **Current**: `Canvas.tsx:234, 235, 308, 309, 318, 319 → 0.015` (6 occurrences)
- **Meaning**: Background layer parallax intensity relative to canvas pan offset. Controls depth illusion.
- **Why extract**: Visual polish parameter, appears 6 times — one source of truth prevents drift.
- **Proposed home**: `src/config/visual.ts → CANVAS_PARALLAX_FACTOR`
- **Proposed DevPanel control**: `slider: 0–0.05, step 0.001` — live
- **Risk**: Medium (6 sites — all need updating)

### [🟡] CANVAS_ZOOM_END_DEBOUNCE_MS
- **Current**: `Canvas.tsx:82 → setTimeout(..., 150)`
- **Meaning**: Delay after last zoom event before "zoom settled" fires — re-enables card pointer events and emits scale to LOD subscribers.
- **Why extract**: Too short = jitter (cards re-enable during active zoom); too long = sluggish recovery.
- **Proposed home**: `src/config/physics.ts → CANVAS_ZOOM_END_DEBOUNCE_MS`
- **Proposed DevPanel control**: `slider: 50–500ms, step 10` — live
- **Risk**: Low

---

## Proposed — P2 Hygiene

### [🟢] GRIMOIRE_SPAWN_Y_OFFSET
- **Current**: `GrimoireSummoner.tsx:94 → y: y.get() + 200`
- **Meaning**: How far below the summoner device the new grimoire card appears.
- **Proposed home**: `src/config/grimoireConfig.ts → GRIMOIRE_SPAWN_Y_OFFSET`
- **Risk**: Low

### [🟢] GRIMOIRE_TIMER_UPDATE_INTERVAL_MS
- **Current**: `Grimoire.tsx:38 → setInterval(tick, 10_000)`
- **Meaning**: How often the countdown timer re-renders (10 sec). Fine for 1-hour grimoires; rough for short debug durations.
- **Proposed home**: `src/config/grimoireConfig.ts → GRIMOIRE_TIMER_UPDATE_INTERVAL_MS`
- **Risk**: Low

### [🟢] GRID_SNAP_SEARCH_RADIUS
- **Current**: `useGridSnap.ts:38 → for (let r = 0; r <= 10; r++)`
- **Meaning**: Max grid cells searched for an empty slot. At 10 cells radius, can resolve ~300 candidates. Too small = cards pile at fallback; too large = search lag.
- **Proposed home**: `src/config/physics.ts → GRID_SNAP_SEARCH_RADIUS`
- **Risk**: Low

### [🟢] GRADE_REVEAL_ANIMATION (duration + scale)
- **Current**: `GrimoireSlotVisual.tsx:130-132 → duration: 0.6, scale: 4`
- **Meaning**: The white "flash" pulse that accompanies grade reveal. Scale 4 = pulse expands to 4× before fading.
- **Proposed home**: `src/config/grimoireConfig.ts → GRADE_REVEAL_ANIM = { DURATION: 0.6, SCALE: 4 } as const`
- **Risk**: Low

### [🟢] SUMMONER_SIZE_PX
- **Current**: `GrimoireSummoner.tsx:143 → w-[300px] h-[300px]`, margins `-150`
- **Meaning**: Diameter of the circular summoner device on the canvas.
- **Proposed home**: `src/config/grimoireConfig.ts → SUMMONER_SIZE_PX = 300`
- **Risk**: Low (Tailwind arbitrary values, 3 sites — marginLeft, marginTop, className)

### [🟢] GRIMOIRE_CARD_SIZE_PX
- **Current**: `Grimoire.tsx:103-104 → w-[120px] h-[160px]`, margins `-60`/`-80`
- **Meaning**: Pixel dimensions of the grimoire book card on the canvas.
- **Proposed home**: `src/config/grimoireConfig.ts → GRIMOIRE_CARD = { W: 120, H: 160 } as const`
- **Risk**: Low

---

## World Size Mismatch — Logged to questions.md (Q1)

`Canvas.tsx` uses `WORLD_W=9600 / WORLD_H=6000`.  
`useGridSnap.ts` uses `WORLD_W=16000 / WORLD_H=10000`.

These should be the same constant. Cards can snap to grid positions the player cannot pan to. **No extraction made** until the correct values are confirmed.

---

## Closing Notes

- Registry at **4 entries** (milestone at 25).
- 4 proposed DevPanel controls across physics domain → consider a "Physics Tuner" session once `src/config/physics.ts` is created and populated.
- The world-size mismatch (Q1) is an actual gameplay bug, not just a param issue — escalate separately.
