# Parameter Session — 2026-04-19 18:00

**Focus: recent** (files changed in last 7 days — second pass)  
**Found: 20 params (🔴1 🟡9 🟢6 ⚪4 skipped) | Auto-extracted: 2 | Proposed: 18 | Registry size: 8 → 10**

**Note:** Last session's 15 Canvas/physics proposals (ZOOM_FRICTION, ZOOM_SENSITIVITY, etc.) are still pending — no extraction has been applied since 2026-04-19-1620. This session continues with new files untouched in the first pass.

Files scanned this session: `Card.tsx`, `CardVisual.tsx`, `RewardCinematicOverlay.tsx`, `useGrimoireExpiry.ts`, `useSynthesis.ts`, `useGrimoireInteraction.ts`, `XPRegistry.ts`, `DefaultGridSystem.tsx`, `Dock.tsx`, `ProgressionHUD.tsx`, `DeviceVisual.tsx`

---

## Auto-Extracted (2)

### [🟢] GRIMOIRE_EXPIRY_POLL_INTERVAL_MS
- **Was**: `const POLL_INTERVAL_MS = 30 * 1000` local to `useGrimoireExpiry.ts:14`
- **Now**: `src/config/grimoireConfig.ts → GRIMOIRE_EXPIRY_POLL_INTERVAL_MS`
- **Why extracted**: Module-local constant with no clear reason to stay there. Centralizing it means a designer can adjust polling frequency when debugging short-duration grimoires without finding the hook.

### [🟢] GRIMOIRE_EXPIRE_ANIMATION_DELAY_MS
- **Was**: `const EXPIRE_ANIMATION_DELAY_MS = 500` local to `useGrimoireExpiry.ts:15`
- **Now**: `src/config/grimoireConfig.ts → GRIMOIRE_EXPIRE_ANIMATION_DELAY_MS`
- **Why extracted**: Pairs with GRIMOIRE_DURATION_MS — if you change how expiry looks/feels, you need both. Should live together in the same file.

---

## Proposed — P0 Tuning-Critical

### [🔴] CARD_LOD_THRESHOLDS (LOD_ENTER / LOD_EXIT)
- **Current**: `Card.tsx:136-137`
  ```ts
  const LOD_ENTER = 0.32;
  const LOD_EXIT  = 0.38;
  ```
- **Meaning**: At canvas zoom < 0.32, cards switch to a lightweight `CompactCardVisual`. Hysteresis prevents flicker at the boundary — cards re-upgrade at 0.38. These two values together define WHEN the game trades visual fidelity for performance.
- **Why extract**: This is the single most impactful knob for quality vs. frame-rate balance. On low-end devices you'd want to enter compact mode earlier (0.5); on high-end you might raise it to 0.25. Currently invisible in config.
- **Proposed home**: `src/config/canvas.ts → CARD_LOD = { ENTER: 0.32, EXIT: 0.38 } as const`
- **Proposed DevPanel control**: Two sliders — ENTER: 0.1–0.8, EXIT: 0.1–0.8 (enforce EXIT > ENTER). Live, no restart.
- **Risk**: Low (pure move — both values are already named)

---

## Proposed — P1 Design-Relevant

### [🟡] REWARD_CINEMATIC_TIMINGS
- **Current**: `RewardCinematicOverlay.tsx:20-22`
  ```ts
  const introTimer = setTimeout(() => setStep('tally'), 1000);
  const tallyTimer = setTimeout(() => setStep('echo'),  3000);
  const echoTimer  = setTimeout(() => setStep('final'), 5500);
  ```
- **Meaning**: Duration of each act in the grimoire reward cinematic — intro holds for 1s, then tally shows for 2s, then echo reveals for 2.5s before allowing dismiss.
- **Why extract**: The cinematic timing is pure feel. If it feels rushed (playtest) or drags (QA), you need to tune three values simultaneously. Currently requires digging into the component.
- **Proposed home**: `src/config/timing.ts → REWARD_CINEMATIC = { INTRO_MS: 1000, TALLY_MS: 3000, ECHO_MS: 5500 } as const`
- **Proposed DevPanel control**: Three sliders (500–6000ms, step 100). Live.
- **Risk**: Low

### [🟡] CARD_HOVER_SCALE / CARD_DRAG_SCALE
- **Current**: `Card.tsx:580 → scaleSpring.set(1.15)` (drag start), `Card.tsx:722 → scaleSpring.set(1.05)` (hover)
- **Meaning**: How much the card grows when hovered (5%) vs. dragged (15%). Controls tactile lift feedback.
- **Why extract**: Two values that directly affect "does this feel satisfying to grab." Hovering at 1.05 is subtle; drag at 1.15 is assertive. Both tunable independently.
- **Proposed home**: `src/config/canvas.ts → CARD_SCALE = { HOVER: 1.05, DRAG: 1.15 } as const`
- **Proposed DevPanel control**: Two sliders (1.0–1.5, step 0.01). Live.
- **Risk**: Medium (scaleSpring.set is called imperatively, not from a memoized reference — if extracted to config, the card must import the value, which is fine; but the import needs to be threaded into the useDrag and onHoverStart callbacks)

### [🟡] CARD_EXPANDED_VIEWPORT_FRACTION
- **Current**: `Card.tsx:362`
  ```ts
  expandedScale.set((Math.min(w, h) * 0.8) / (Math.max(width, height) * safeScale));
  ```
- **Meaning**: When a card is expanded (clicked), it fills 80% of the viewport's smaller dimension. Controls how "focused" the expanded card feels — lower = more context, higher = more readable.
- **Why extract**: 80% is a design judgment. On mobile you'd want 90%; with lots of cards visible you might want 60%.
- **Proposed home**: `src/config/canvas.ts → CARD_EXPANDED_VIEWPORT_FRACTION = 0.8`
- **Proposed DevPanel control**: Slider 0.4–1.0, step 0.05. Live.
- **Risk**: Low

### [🟡] SYNTHESIS_AUTOPOLL_DELAYS
- **Current**: `useSynthesis.ts:75 → const delays = [25_000, 25_000, 50_000]`
- **Meaning**: After synthesis, if the visual isn't ready yet, the client polls at 25s → 25s → 50s intervals (total: 100s before giving up). Controls how quickly a user sees their card art — and how much read traffic the DB gets.
- **Why extract**: If AI generation gets faster (or slower), these need to change. Currently requires code search to find.
- **Proposed home**: `src/config/timing.ts → SYNTHESIS_POLL_DELAYS_MS = [25_000, 25_000, 50_000] as const`
- **Proposed DevPanel control**: Three number inputs (5000–120000ms). Not live — only affects next synthesis attempt.
- **Risk**: Medium (array value, used in a loop — must preserve as array)

### [🟡] SYNTHESIS_HARD_TIMEOUT_MS
- **Current**: `useSynthesis.ts:165 → setTimeout(..., 90_000)`
- **Meaning**: Hard ceiling on how long a synthesis attempt can block. After 90s, the request is considered failed and the circle is freed. Prevents the UI from freezing indefinitely.
- **Why extract**: 90s is a rough guess for edge function cold starts + Gemini latency. As infra improves or degrades, this should be adjustable. Currently buried in the hook.
- **Proposed home**: `src/config/timing.ts → SYNTHESIS_HARD_TIMEOUT_MS = 90_000`
- **Proposed DevPanel control**: Slider 30000–300000ms, step 5000. Not live — affects next synthesis.
- **Risk**: Low

### [🟡] XP_SOURCE_FACTORS (duplicate fraction, evolution multiplier)
- **Current**: `XPRegistry.ts:88-89`
  ```ts
  case 'SENSE_DUPLICATED': return Math.floor(XP_SENSE_BASE * 0.1);
  case 'EVOLUTION_SUCCESS': return Math.floor(XP_SENSE_BASE * coef * 2);
  ```
- **Meaning**: Duplicate sense earns only 10% of base XP (discourages farming the same word). Evolution earns 2× base with CEFR bonus (rewards harder-word mastery). These factors define the XP economy's shape.
- **Why extract**: These multipliers will need rebalancing during playtesting. Currently scattered inline with no single-source view of the XP economy.
- **Proposed home**: `src/config/balance.ts → XP_FACTORS = { DUPLICATE_FRACTION: 0.1, EVOLUTION_MULTIPLIER: 2 } as const`
- **Proposed DevPanel control**: Two sliders (0.0–5.0, step 0.05). Not live — affects next award.
- **Risk**: Low

### [🟡] XP_CONSTRUCTION_FIXED
- **Current**: `XPRegistry.ts:90 → return 50; // 暂定固定值`
- **Meaning**: Flat XP for creating a construction (phrase/sentence). The "temporary" comment signals this was never properly balanced. 50 XP is between C and B grimoire reward — possibly too high if constructions become spammable.
- **Why extract**: The "TODO: balance" comment explicitly flags this for later tuning.
- **Proposed home**: `src/config/balance.ts → XP_CONSTRUCTION_FIXED = 50`
- **Proposed DevPanel control**: Slider 10–200, step 5. Not live.
- **Risk**: Low

### [🟡] GRID_SMALL_CELL_SIZE / GRID_LARGE_CELL_SIZE
- **Current**: `DefaultGridSystem.tsx:47,50`
  ```ts
  <pattern id="smallGrid" width="100" height="100" ...>
  <pattern id="largeGrid" width="1000" height="1000" ...>
  ```
- **Meaning**: The fine-grain grid (100px cell) and coarse grid (1000px cell = 10×10 fine cells). The coarse grid is also the major axis marker. These should match the card grid from useGridSnap (GRID_CELL_W=280, GRID_CELL_H=380) for the grid to feel aligned to card placement — currently it doesn't.
- **Why extract**: Visual grid and card snap grid are decoupled. Extracting these makes it possible to align them. When the snap grid changes, the visual grid must update too.
- **Proposed home**: `src/config/canvas.ts → GRID_VIS = { SMALL_CELL: 100, LARGE_CELL: 1000 } as const`
- **Proposed DevPanel control**: Two sliders (50–500px). Live (SVG rerenders).
- **Risk**: Low (SVG pattern values only, no snap logic here)

### [🟡] GRID_OPACITY_TRANSFORMS
- **Current**: `DefaultGridSystem.tsx:16-17`
  ```ts
  const smallGridOpacity = useTransform(scale, [0.1, 0.4, 1.5], [0, 0.2, 0.4]);
  const largeGridOpacity = useTransform(scale, [0.1, 1],       [0.3, 0.6]);
  ```
- **Meaning**: The fine grid fades in as you zoom in (invisible at ≤0.1, max opacity 0.4 at scale 1.5). The coarse grid stays partially visible even at full zoom-out. Controls how busy the background looks at different zoom levels.
- **Why extract**: Three input + three output values for small, two + two for large. All affect visual noise. A player complaining "the grid is distracting" means reducing max opacities; "can't see where to place cards" means increasing them.
- **Proposed home**: `src/config/canvas.ts → GRID_OPACITY = { SMALL: { input: [0.1, 0.4, 1.5], output: [0, 0.2, 0.4] }, LARGE: { input: [0.1, 1], output: [0.3, 0.6] } } as const`
- **Proposed DevPanel control**: Individual sliders for each opacity endpoint. Live.
- **Risk**: Medium (nested structure)

---

## Proposed — P2 Hygiene

### [🟢] CARD_ANIMATION_LOCKOUT_MS
- **Current**: `Card.tsx:201 → setTimeout(() => setIsAnimating(false), 600)`
- **Meaning**: How long after a merge/split event the card ignores new hover/interaction (prevents visual glitch during animation).
- **Proposed home**: `src/config/canvas.ts → CARD_ANIMATION_LOCKOUT_MS = 600`
- **Risk**: Low

### [🟢] GRIMOIRE_EXPIRY_POLL_INTERVAL_MS ✓ (already extracted this session)

### [🟢] GRIMOIRE_EXPIRE_ANIMATION_DELAY_MS ✓ (already extracted this session)

### [🟢] DOCK_ZOOM_SCALE
- **Current**: `Dock.tsx:109 → zoomedScaleMultiplier.set(isZoomed ? 0.75 : 1)`
- **Meaning**: When a card is expanded, the dock shrinks to 75% and fades. Controls how much the HUD recedes to let the expanded card breathe.
- **Proposed home**: `src/config/canvas.ts → DOCK_ZOOMED_SCALE = 0.75`
- **Risk**: Low

### [🟢] DEVCONSOLE_MESSAGE_BUFFER / LOG_BUFFER
- **Current**: `DevConsole.tsx:122 → prev.slice(-99)` (message ring buffer size 99), `DevConsole.tsx:140 → logger.getHistory().slice(-100)` (log buffer 100)
- **Meaning**: How many messages / log entries the DevConsole holds in memory. Affects how far back you can scroll the stream.
- **Proposed home**: ⚪ Leave it — these are dev-tooling internals, not game parameters.
- **Risk**: N/A

---

## Skipped ⚪

- `LINE_HEIGHT = 40` (Canvas.tsx:248) — browser standard for CSS line-height; this is a DOM convention, not a game param.
- `50` discrete wheel threshold (Canvas.tsx:259) — browser device heuristic; tuning it breaks cross-device compatibility.
- `800` / `200` center mark circle radii (DefaultGridSystem.tsx) — purely decorative SVG, no gameplay connection.
- `0.999` opacity hack (Card.tsx:499) — GPU rasterization workaround, must not change.

---

## Data Quality Flag — XPRegistry vs GRIMOIRE_REWARDS Mismatch (→ questions.md Q2)

`XPRegistry.ts:94-96` has an inline `gradeMap` for grimoire XP:
```ts
'S++': 500, 'S+': 400, 'S': 300, 'A': 200, 'B': 100, 'C': 50, 'D': 25, 'F': 0
```

`src/config/grimoireConfig.ts:83` has `GRIMOIRE_REWARDS` with different XP values:
```ts
'S++': 150, 'S+': 110, 'S': 80, 'A': 55, 'B': 35, 'C': 20, 'D': 10, 'F': 0
```

These are not the same and both claim to represent "XP earned for grimoire completion."  
**Logged to questions.md as Q2** — do not extract either set until the intended values are confirmed.

---

## Closing Notes

- Registry at **10 entries** (milestone at 25).
- Last session's 15 proposals (Canvas physics, CEFR thresholds, timer warnings) are still open — 17 total proposals now pending. If the user wants a wiring session after extraction, there's material for a full Physics Tuner panel.
- `src/config/visual.ts` does not yet exist — the CANVAS_PARALLAX_FACTOR (last session) and GRID_OPACITY_TRANSFORMS (this session) would go there. Create when 3+ visual params are confirmed for extraction.
- XP data inconsistency (Q2) is a balance bug, not just a param issue — worth escalating to the balance review backlog.
