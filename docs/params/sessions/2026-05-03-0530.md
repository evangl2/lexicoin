# Parameter Session — 2026-05-03 05:30

**Focus: recent** (files changed in last 7 days — second pass)
**Found: 2 params (🔴0 🟡0 🟢2 ⚪0) | Auto-extracted: 2 | Proposed: 0 | Registry size: 10 → 12**

Files scanned this session: `Card.tsx`, `Dock.tsx`.
Note: Earlier findings were already documented in session report `2026-04-19-1800.md`. This session specifically applies the wiring of two P2 parameters.

---

## Auto-Extracted (2)

### [🟢] CARD_ANIMATION_LOCKOUT_MS
- **Was**: `const CARD_ANIMATION_LOCKOUT_MS = 600` inside `src/app/components/ui/card/Card.tsx`
- **Now**: `src/config/canvas.ts → CARD_ANIMATION_LOCKOUT_MS`
- **Why extracted**: Defines how long a card ignores hover interactions after merge/split animation. Avoids visual glitches, but if animations are tuned, this value should be in a centralized location for alignment.

### [🟢] DOCK_ZOOMED_SCALE
- **Was**: `zoomedScaleMultiplier.set(isZoomed ? 0.75 : 1)` inside `src/app/components/ui/shell/Dock.tsx`
- **Now**: `src/config/canvas.ts → DOCK_ZOOMED_SCALE`
- **Why extracted**: Defines how much the HUD dock recedes when expanding a card. Centralizing this value improves consistency if other HUD elements also need to react to zoom.

---

## Closing Notes
- Registry crossed the 10 entries mark. (Current: 12)
- No further proposals generated in this limited targeted pass.
