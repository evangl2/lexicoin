# Daily Docs Sync — 2026-04-19 (Run 2)

**Commits processed: 4 (960d072 param-extract, 1d570a9 refactor-docs, 39395ac feel-docs, f1a6be0 nanleveling) | Reactive: 🔴0 🟡1 🟢0 ⚪0 | Audit sample: [BackendSchema.md, CompactModeSystem.md, card-data-pipeline.md] | Auto-applied: 3 | Proposed: 3 | Open questions: 0**

---

## Context: What Changed Since Run 1

### Commit `960d072` (Jules — param extraction)
Extracted two hardcoded constants into new config files:
- `src/config/timing.ts` — `SYNTHESIS_LONG_STATE_DELAY_MS = 15000`
- `src/config/physics.ts` — `HUD_PROGRESS_BAR_SPRING = { stiffness: 50, damping: 20 }`
Also updated `src/app/hooks/useSynthesis.ts` and `src/app/components/ui/shell/ProgressionHUD.tsx` to import from these files. Session report at `docs/params/sessions/2026-04-19-0502.md`.

### Commit `1d570a9` (Jules — refactor docs)
Created `docs/refactor/` with 5 files: `backlog.md`, `principles.md`, `questions.md`, `sessions/2026-04-19-1620.md`, `weekly/2026-16.md`.

### Commit `39395ac` (Jules — game feel docs)
Created `docs/feel/` with 4 files: `backlog.md`, `daily/2026-04-19.md`, `principles.md`, `questions.md`.

### Commit `f1a6be0` (user — nanleveling)
Modified `src/app/components/ui/shell/ProgressionHUD.tsx`. No doc impact (behavior tweak, not API change).

---

## Previous Run Follow-up

### [✅] `docs/persona-system.md` 🔴 P0 — RESOLVED
The broken persona paths flagged in Run 1 were fixed by the user in commit `df05e08`. The doc now correctly describes the `_shared/personas/` structure (CHILD/GARDENER/ALCHEMIST). No further action needed.

---

## Mode A — Reactive Sync

### [🟡] PROPOSED: `docs/gameconfig.md` — missing `timing.ts` and `physics.ts`
- **File**: `docs/gameconfig.md`
- **Trigger**: `src/config/timing.ts` and `src/config/physics.ts` added in 960d072
- **Change summary**: `gameconfig.md` currently lists 4 config files (canvas, balance, grimoireConfig, constants). Two new files were added and are not documented.
- **Proposed addition** (append after `constants.ts` section):

```markdown
## `timing.ts` — 时序常量

| 导出 | 默认值 | 说明 |
|------|--------|------|
| `SYNTHESIS_LONG_STATE_DELAY_MS` | `15000` | 合成超时后显示"还在处理中"状态的等待时长（ms） |

**消费方**：`useSynthesis.ts`。

---

## `physics.ts` — 动画物理常量

| 导出 | 默认值 | 说明 |
|------|--------|------|
| `HUD_PROGRESS_BAR_SPRING` | `{ stiffness: 50, damping: 20 }` | ProgressionHUD 进度条和体力条的弹簧动画参数 |

**消费方**：`ProgressionHUD.tsx`。
```

Also update the directory tree at the top:
```
src/config/
├── canvas.ts
├── balance.ts
├── grimoireConfig.ts
├── timing.ts         ← 时序常量（新）
├── physics.ts        ← 动画物理（新）
└── constants.ts
```
- **Risk**: Low

---

## Mode B — Freshness Audit

### [🔴] PROPOSED: `docs/CompactModeSystem.md` §5 — all file paths broken
- **File**: `docs/CompactModeSystem.md`
- **Trigger**: Freshness audit; 2026-03 restructure moved all listed files
- **Change summary**: The §5 File Structure section shows 4 paths that are all wrong after the restructure. `RepoCard.tsx` no longer exists as a separate file (its logic is absorbed into `DeckRepository.tsx`).

**Current §5 (broken)**:
```
src/
  app/
    components/
      ui/
        CompactCardVisual.tsx  // ← does not exist here
        DeckRepository.tsx     // ← does not exist here
        RepoCard.tsx           // ← does not exist at all
        DragPreviewCard.tsx    // ← does not exist here
```

**Correct paths**:
```
src/app/components/ui/
├── card/
│   ├── CompactCardVisual.tsx   // 核心渲染组件 (Stateless)
│   └── DragPreviewCard.tsx     // 拖拽时的替身组件
└── shell/
    └── DeckRepository.tsx      // 容器组件，管理 List 渲染与模式切换
                                // (RepoCard 逻辑已内联至此，不再独立文件)
```

The §3.2 prose mentions `RepoCard.tsx` → `useCardVariants` flow. This may also need updating if the component was truly absorbed, but I can't verify behavior from file inspection alone — flag for you to confirm.
- **Risk**: Medium (path fix is safe; the `RepoCard` absorption affects §3.2 prose — propose, not auto-apply)

---

### [🟡] PROPOSED: `docs/BackendSchema.md` — stale persona examples
- **File**: `docs/BackendSchema.md`
- **Trigger**: Freshness audit; persona system renamed in 0418 commit
- **Change summary**: The `sense_flavor_texts` table description uses "The Joker" and "The Prophet" as example persona IDs. The persona system now uses CHILD, GARDENER, ALCHEMIST.
- **Diff**:

```diff
-| `persona` | `text` | - | AI Persona ID (e.g., "The Joker", "The Prophet"). |
+| `persona` | `text` | - | AI Persona ID (e.g., "CHILD", "GARDENER", "ALCHEMIST"). |
```
- **Risk**: Low — one-line factual update, no prose rewrite
- **Note**: This is a factual correction in a technical doc, not author-owned prose — could auto-apply on next run if you confirm the stored DB values match CHILD/GARDENER/ALCHEMIST (i.e., the seeds/migrations were updated too).

---

### [⚪] `docs/card-data-pipeline.md` — accurate
- **Audited**: 2026-04-19
- **Verdict**: All referenced paths verified:
  - `src/core/storage/SenseRepository.ts` ✓
  - `src/core/pipelines/senseToCard.ts` ✓
  - `src/app/hooks/useCardManager.ts` ✓
  - `src/schemas/data/initialSenses.ts` ✓
- Content and data flow diagram look current. No action needed.

---

## Auto-Applied

1. `docs/_meta/index.md` — added `docs/refactor/` section (5 files), `docs/feel/` section (4 files), `docs/params/sessions/2026-04-19-0502.md`; marked BackendSchema/CompactModeSystem/card-data-pipeline/gameconfig audit dates
2. `docs/_meta/audit-rotation.md` — updated BackendSchema, CompactModeSystem, card-data-pipeline to 2026-04-19
3. `docs/_meta/backlog.md` — added 3 proposed actions for Run 2

---

**Docs tracked: 46 | Audited today (Run 2): 3 (BackendSchema, CompactModeSystem, card-data-pipeline) | Oldest unaudited: DataManagement.md, DB-schema-maintenance.md, DurabilityLifecycle.md (all at "—")**

**Attention needed**: `docs/CompactModeSystem.md` §5 is 🔴 P0 — all four file paths are wrong after the 2026-03 restructure, and `RepoCard.tsx` no longer exists. Confirm whether §3.2 prose (the RepoCard → useCardVariants flow) still reflects actual behavior, then I can draft the full replacement.
