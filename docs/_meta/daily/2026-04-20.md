# Daily Docs Sync — 2026-04-20

**Commits processed: 7 (2 user code: 97da189 "0420", b0d6fb7 "0420update"; 2 Jules bot doc: feel+entropy; 3 merges) | Reactive: 🔴0 🟡0 🟢2 ⚪0 | Audit sample: [DataManagement, DB-schema-maintenance, DurabilityLifecycle, dynamic_text_feature, genui-architecture] | Auto-applied: 3 | Proposed: 3 | Open questions: 0**

---

## Context: What Changed

### Commit `b0d6fb7` (user — 0420update)
Large pre-refactor update. New author docs: `docs/grimoire/ActionItems_Grimoire_Test.md`, `docs/grimoire/E2E_TestJourney.md`, `docs/refactor/card-web-component/prd.md`, `tdd.md`, `wbs.md` (all author-owned). Code changes: `App.tsx`, `Card.tsx`, `ConfigMenu.tsx`, `GrimoireSummoner.tsx`, `SynthesisCircle.tsx`, `useViewportCulling.ts`.

### Commit `97da189` (user — 0420)
Two substantive new source files and a new author doc:
- **`src/core/store/slices/featureFlags.ts`** (NEW) — Feature flags Zustand slice; introduces `useWCCards: boolean` for the upcoming web-component card refactor
- **`supabase/functions/_shared/callAI.ts`** (NEW) — Unified AI call middleware: routes `gemini-*` to Google SDK, others to OpenRouter; 60 s timeout, 2-attempt retry, Gemini `responseSchema` support
- **`docs/refactor/card-web-component/_tailwind-mapping.md`** (NEW, author-owned) — Tailwind class mapping for the WC refactor

`evaluate-grimoire/index.ts` and `generate-grimoire/index.ts` were both refactored to consume `callAI` (previously each had inline Gemini logic).

### Jules bot commits
- `docs/feel/daily/2026-04-20.md` added (card interactions game-feel report)
- `docs/entropy/` directory created with `weekly/2026-16.md`, `backlog.md`, `ignore.md`, `questions.md`

---

## Mode A — Reactive Sync

### [🟢] PROPOSED: New doc for `supabase/functions/_shared/callAI.ts`
- **File**: would go in `docs/technical/callAI.md` (new file, ~30 lines)
- **Trigger**: `callAI.ts` added in 97da189 — 189-line shared module now consumed by all three edge functions
- **Change summary**: Single entry point for all AI calls across Edge Functions. Routing logic, retry behavior, and env var requirements are non-obvious and worth documenting centrally.
- **Proposed stub**:

```markdown
## Edge Function AI 调用中间层 (`supabase/functions/_shared/callAI.ts`)

统一入口，替代各函数内的内联 Gemini 调用。

### 路由规则
| model 参数 | 后端 | 所需 env var |
|------------|------|-------------|
| `gemini-*` | Google Generative AI SDK | `GEMINI_API_KEY` |
| 其他 | OpenRouter REST (OpenAI 兼容) | `OPENROUTER_API_KEY` |

默认 model：`gemini-3.1-flash-lite-preview`。

### 主要特性
- **60 s 超时**：防止 Edge Function 永久挂起
- **自动重试**：Attempt 1 失败后自动 Attempt 2，两次均失败才抛出
- `responseSchema`：仅 Gemini 支持，用于强结构化输出（如 `generate-grimoire`）
- OpenRouter 调用时 `responseMimeType='application/json'` → `response_format: {type:'json_object'}`

### 消费方
- `evaluate-grimoire/index.ts`
- `generate-grimoire/index.ts`
- `synthesize-sense/index.ts`
```

- **Risk**: Low (new content, no existing text changed)

---

### [🟢] PROPOSED: Document `src/core/store/slices/featureFlags.ts`
- **File**: recommend appending to `docs/gameconfig.md` (already covers `src/config/` constants; this is the store-level equivalent)
- **Trigger**: `featureFlags.ts` added in 97da189, wired into `GameStore` via `interfaces.ts`
- **Change summary**: New Zustand slice introducing a runtime feature flag system. Currently one flag: `useWCCards` (default `false`), which gates the card web-component refactor described in `docs/refactor/card-web-component/`.
- **Proposed addition** (append to `docs/gameconfig.md`):

```markdown
## Feature Flags (`src/core/store/slices/featureFlags.ts`)

Runtime feature toggles stored in Zustand (not persisted to IndexedDB).

| Flag | Type | Default | 用途 |
|------|------|---------|------|
| `useWCCards` | `boolean` | `false` | 启用 Web Component 版卡片渲染（与 `docs/refactor/card-web-component/` 重构关联） |

**读取**：`useGameStore(s => s.featureFlags.useWCCards)`  
**写入**：`useGameStore(s => s.setFeatureFlag)('useWCCards', true)`
```

- **Risk**: Low

---

## Mode B — Freshness Audit

### [🔴] AUTO-APPLIED: `docs/dynamic_text_feature.md` §6.1 — broken import path
- **Audited**: 2026-04-20
- **Trigger**: 2026-03 restructure moved `TieredText.tsx` from `ui/` to `ui/text/`
- **Change summary**: The §6.1 code example had a stale import path. Fixed in place.
- **Diff**:
```diff
-import { TieredText } from '@/app/components/ui/TieredText';
+import { TieredText } from '@/app/components/ui/text/TieredText';
```
- **Risk**: Low (code example in a technical doc, single-line fix, actual file verified at `src/app/components/ui/text/TieredText.tsx`)

---

### [🟡] PROPOSED: `docs/DataManagement.md` — wrong Dexie version
- **Audited**: 2026-04-20
- **Trigger**: Freshness audit; `package.json` shows `"dexie": "^4.3.0"`
- **Change summary**: The doc's opening paragraph says "Dexie V6 引擎" — the actual version is v4. `DB-schema-maintenance.md` correctly says "Dexie v4". Minor but would confuse anyone checking the version.
- **Diff**:
```diff
-借助底层的高性能 IndexedDB 库 (`db.ts` 与 Dexie V6 引擎)
+借助底层的高性能 IndexedDB 库 (`db.ts` 与 Dexie v4 引擎)
```
- **Risk**: Low — one-word factual correction in author prose (proposing, not auto-applying because it's author-written narrative text)

---

### [⚪] `docs/DB-schema-maintenance.md` — accurate
- **Audited**: 2026-04-20
- **Verdict**: All content verified. Header correctly says "Dexie v4 + IndexedDB". Schema listed as v7. All referenced file paths (`src/core/storage/db.ts`, `CardInventoryRepository`, etc.) verified to exist. No action needed.

### [⚪] `docs/DurabilityLifecycle.md` — accurate
- **Audited**: 2026-04-20
- **Verdict**: `DurabilitySystem.ts` ✓, `CardInventoryRepository.ts` ✓, `useCardManager.ts` ✓, `CARD_DEPLETED` / `CARD_DURABILITY_CHANGED` events ✓ (in `src/types/protocol.ts`). Content accurately describes the three-step depletion sequence. No action needed.

### [⚪] `docs/genui-architecture.md` — accurate
- **Audited**: 2026-04-20
- **Verdict**: Conceptual/design doc with no specific file paths to verify. Content describes best-practice GenUI pipeline (validation → sandbox → error boundaries). Still valid design guidance. No action needed.

---

## Auto-Applied

1. `docs/dynamic_text_feature.md` — fixed stale import path in §6.1 code example (`ui/TieredText` → `ui/text/TieredText`)
2. `docs/_meta/index.md` — registered 11 new files (grimoire: 2, refactor/card-web-component: 4, feel: 1, entropy: 4, perf: 2 previously missing); merged orphaned duplicate params section; updated audit dates for 5 audited docs
3. `docs/_meta/audit-rotation.md` — updated DataManagement, DB-schema-maintenance, DurabilityLifecycle, dynamic_text_feature, genui-architecture to 2026-04-20

---

**Docs tracked: 62 | Audited today: 5 | Oldest unaudited: InflectionSystem.md, infrastructure_prd.md, infrastructure_tdd.md (all at "—")**

**No urgent attention needed.** All 🔴 issues from prior runs were resolved. Two 🟢 gaps proposed today (callAI doc + featureFlags doc) are low-stakes — they document new infra added as part of the WC card refactor, and the proposed stubs are ready to apply when you confirm the home file.
