Week 17 | Recent scope: 111 files | Deep sweep: Category F (Bloat) | Found: A:0 B:0 C:0 D:0 E:1 F:7 | Proposed: 8 | Auto-applied: 0 | Principles active: 0

### 🟡 Extract Deck Repository Sub-components
- **Category**: F
- **Location**: `src/app/components/ui/shell/DeckRepository.tsx`
- **Current**: A 700-line file that includes the main `DeckRepository` component alongside multiple sub-components like `RepoModeButton`, `EmptyState`, `RepoTabButton`, `RepoDevice`, and `RepoProp`.
- **Proposed**: Extract the sub-components (`RepoModeButton`, `EmptyState`, `RepoTabButton`, `RepoDevice`, `RepoProp`) into their own files or a `components/` sub-folder within `shell/` to reduce the main file's bloat and improve maintainability.
- **Principle**: Splitting large UI files with multiple distinct components improves readability and separation of concerns.

### 🟡 Extract DevConsole Sub-components/Logic
- **Category**: F
- **Location**: `src/app/components/system/DevConsole.tsx`
- **Current**: A 679-line file that manages the developer console UI, various tabs (Cards, Progression, Database, Memory, Logs), and associated state.
- **Proposed**: Extract the individual tab panels (e.g., `CardsTab`, `ProgressionTab`, `DatabaseTab`) into separate components to reduce the main file size and complexity.
- **Principle**: Breaking down a mega-component that handles many unrelated concerns (different dev tools) into smaller, focused components makes the code easier to navigate and maintain.

### 🟡 Extract ScrollSelect from ConfigMenu
- **Category**: F
- **Location**: `src/app/components/ui/shell/ConfigMenu.tsx`
- **Current**: The `ScrollSelect` component (approx 150 lines) is defined within the same file as `ConfigMenu`, inflating its size to 566 lines.
- **Proposed**: Move `ScrollSelect` to a shared UI component file (e.g., `src/app/components/ui/shared/ScrollSelect.tsx`) so it can be reused elsewhere and to simplify `ConfigMenu.tsx`.
- **Principle**: Extracting complex UI components (especially custom selects with portals and animations) to shared libraries reduces bloat and encourages reuse.

### 🟡 Merge `Card.tsx` and `CardWCSlots.tsx` state
- **Category**: F
- **Location**: `src/app/components/ui/card/Card.tsx`, `src/app/components/ui/card/CardWCSlots.tsx`
- **Current**: `Card.tsx` is 432 lines, and it heavily relies on `CardWCSlots.tsx` (403 lines) which takes a huge list of props to construct DOM slots for a web component.
- **Proposed**: Extract the state logic and hook wiring from `Card.tsx` into a `useCardState` hook, and move the presentation logic into smaller sub-components for the front/back faces.
- **Principle**: Separating data/state from presentation logic reduces file size and makes the mega-component easier to test and modify.

### 🟡 Consolidate Card State Management Hooks
- **Category**: E
- **Location**: `src/app/hooks/useCardManager.ts`, `src/app/hooks/useCardGrouping.ts`
- **Current**: `useCardManager` (416 lines) manages basic loading, saving, and moving cards between repo/canvas. `useCardGrouping` (427 lines) manages complex grouping/merging logic. They both manipulate the same underlying card state but are separated, causing complex data flows and synchronization issues.
- **Proposed**: Combine `useCardManager` and `useCardGrouping` into a single cohesive `useCards` hook, or move the core state to Zustand and have both hooks act as selectors/actions on the store.
- **Principle**: State that changes together should live together. Splitting tightly coupled state logic across multiple custom hooks leads to artificial complexity and prop drilling.

### 🟡 Refactor Edge Function Handlers
- **Category**: F
- **Location**: `supabase/functions/synthesize-sense/index.ts`
- **Current**: A massive 890-line Edge Function file that handles DB logic, cache checking, JSON repair, LLM routing, and HTTP response handling.
- **Proposed**: Split the file into focused modules: e.g., `cache.ts`, `utils.ts`, `db.ts`, leaving `index.ts` as a clean HTTP controller. *Note: this requires modifying Supabase Edge Functions which may have deployment impacts.*
- **Principle**: Monolithic edge functions are hard to debug and test. Separating concerns makes the code easier to maintain and test locally.

### ⚪ Split Semantic Module Types
- **Category**: F
- **Location**: `src/schemas/schemas/SenseEntity.schema.ts`
- **Current**: A massive 716-line schema file containing `SenseEntity`, `QualiaStructure`, `VisualEntry`, and many other semantic core types, heavily documented.
- **Proposed**: Extract logical groups (e.g., `Qualia`, `Visual`, `Shell`) into separate schema files within `src/schemas/schemas/` to reduce bloat, while keeping `SenseEntity.schema.ts` as the root composition file.
- **Principle**: Splitting large schema files by domain concept improves readability and reduces merge conflicts when updating specific semantic structures.

### ⚪ Extract Initial Senses into JSON or Individual Files
- **Category**: F
- **Location**: `src/schemas/data/initialSenses.ts`
- **Current**: A 653-line TypeScript file exporting massive, hardcoded SenseEntity objects.
- **Proposed**: Move the raw data out of TypeScript into `src/schemas/data/InitialItem/*` JSON files or separate TS files per sense, exporting them via an `index.ts` barrel.
- **Principle**: Large data payloads in TS files degrade editor performance and mix data with logic. Extracting them to JSON or smaller files keeps the codebase modular.

Session saved: docs/refactor/sessions/2025-17.md
Principles library: 0 entries
