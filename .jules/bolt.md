## 2024-03-03 - [Missing tsc]
**Learning:** The project fails `npm run type-check` and `npx tsc` because tsc isn't available. Tests can't run as expected.
**Action:** Rely on static analysis and manual review if tsc doesn't work, according to the memory.
